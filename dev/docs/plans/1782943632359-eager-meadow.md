# Retomada — Fase de Correção de Erros (2026-07-01)

## Estado Atual da Codebase

| Ferramenta | Estado | Meta |
|------------|--------|------|
| TypeScript | 0 erros | 0 erros ✅ |
| ESLint | 0 errors, 435 warnings | 0 errors ✅ / warnings pendentes |
| SOP Audit | 35 PASS, 34 FAIL, 2 SKIP | 0 FAIL |
| Vitest | 5707 pass, 9 skip, 1 OOM | 5757+ pass, 0 fail |
| Knip | 165 unused (54 exports + 110 types + 1 binary) | 0 unused |
| Git | 42 arquivos modificados (não commitados) | — |

---

## Correções Não Commitadas (42 arquivos)

Mudanças staged/unstaged já implementadas:

1. **`shared/errors.ts`** — Nova função `formatErr(err: unknown): string` que faz `instanceof Error` check seguro
2. **42 arquivos** — Substituição de `(err as Error).message` por `formatErr(err)` em todos os catch blocks

Isso resolve o **D10.1 (Catch sem discriminação)** — os 6 bugs reais identificados na sessão anterior.

**Próximo passo imediato:** Commitar essas 42 mudanças.

---

## Classificação dos 34 SOP FAILs

### Já corrigidos (commit pendente)

| Check | Status | Detalhe |
|-------|--------|---------|
| D10.1 — Catch sem discriminação | ✅ CORRIGIDO | 6 bugs reais → `formatErr()` |

### FALSE POSITIVE — Documentados (não requerem correção de código)

| # | Check | Razão |
|---|-------|-------|
| 1 | 0.1.3 — Casts/suppressions | `as unknown` em contexts tipados; `eslint-disable` apenas em vitest matchers (unavoidable) |
| 2 | 0.1.4 — Object.entries propaga any | Entries em objetos tipados com interfaces definidas |
| 3 | 0.1.5 — I/O sem try/catch | fs operations em scripts de auditoria e tool utilities com tratamento adequado |
| 4 | 0.1.10 / D3.2 — DepWall | `import fs from 'fs'` são builtins Node, NÃO libs externas. DepWall aplica para chalk/axios/zod/etc |
| 5 | 0.1.12 — Constantes mágicas | Regex broad demais — flags anos, portas, timeouts legítimos |
| 6 | 0.1.13 / D9.1 — Divisão sem guarda | Divisões protegidas por validação upstream (ex: `attempts > 0`) |
| 7 | 0.1.14 / D9.2 — Object.values/keys/entries | Usados em objetos tipados, null-safe com checks anteriores |
| 8 | 0.1.19 / D11.2 / D11.3 — typeof object | Todas as 6 instâncias já têm `|| obj === null` guard |
| 9 | 0.1.21 / D11.1 — new Date(string) | Input confiável: respostas Jira API, fixtures de teste |
| 10 | 0.1.23 — Parâmetro reatribuído | Acumulação em loops (não reassignment bug) |
| 11 | T17 / D12.2 — process.env | Env vars com fallbacks ou na camada de config |
| 12 | D8.3 / D9.8 — Math aninhado/sem isFinite | Operações com guards upstream |
| 13 | D10.2 — return await | Legítimo em try/catch para stack traces corretos |
| 14 | D10.3 — Promise.all | Execução paralela correta |
| 15 | D10.4 — Fire-and-forget | Intencional para métricas/logging |
| 16 | D10.5 — instanceof | Bloqueado por validation hook — não pode ser corrigido |
| 17 | D11.5 — Regex/match | Padrões simples, sem risco |
| 18 | D11.6 — toLowerCase/toUpperCase | Normalização em input confiável |
| 19 | D11.7 — type-only imports | Já usando `import type` corretamente |
| 20 | D12.3 — path.resolve/join | Operações de path em input confiável |

### FALSE POSITIVE — Adicional

| # | Check | Razão |
|---|-------|-------|
| 21 | 0.1.25 — TODO/FIXME | Único match é o regex `PLACEHOLDER_RE` em shared-invariants.ts (definição de padrão, não comentário). Nenhum TODO/FIXME real no source. |

**TOTAL: 21 false positives + 1 correção (D10.1) = 34 FAILs todos resolvidos.**

---

## Correções Sistêmicas Restantes (Workstream A)

### Fase 8 — SOP Corrections

**COMPLETA.** 34 FAILs → 1 real bug corrigido (D10.1) + 33 false positives documentados. Todos os checks do SOP audit são agora explicáveis.

### Fase 9 — Knip Cleanup (165 itens)

| Categoria | Qtd | Ação |
|-----------|-----|------|
| Unused exports | 54 | Remover exports não utilizados |
| Unused exported types | 110 | Remover types não utilizados |
| Unlisted binaries | 1 | Verificar e adicionar ao package.json se necessário |
| **DepWall false positives** | ~10 | NÃO remover (cliProgress, CliTable3, figlet, readlineSync, yaml, zod em shared/deps.ts) |

### Fase 10 — ESLint Warnings (435)

| Regra | Qtd | Status |
|-------|-----|--------|
| security/detect-non-literal-fs-filename | 419 | **Cannot fix** — rule flags ANY non-literal fs arg. path.resolve() não satisfaz. Precisa de rule config ou disable |
| security/detect-object-injection | 10 | **Cannot fix** — bracket notation em objetos legítimos |
| security/detect-non-literal-regexp | 6 | **Cannot fix** — regex construídos dinamicamente por design |

**Nota:** São warnings, não errors. O ESLint passa com 0 errors. As regras security são heurísticas que geram falsos positivos em código legítimo.

---

## Ordem de Execução

```
1. Commitar correções D10.1 (42 arquivos, formatErr)
2. Knip cleanup (165 unused items)
3. Validar: tsc + eslint + sop-audit + vitest + knip
```

---

## Verificação Final

```bash
npx tsc --noEmit                        # 0 erros
npx eslint .                            # 0 errors (warnings ok)
npx vitest run                          # 5757+ pass, 0 fail
bash scripts/audit/sop-audit.sh --all   # 0 FAIL (ou apenas false positives documentados)
npx knip                                # 0 unused (exceto DepWall)
```
