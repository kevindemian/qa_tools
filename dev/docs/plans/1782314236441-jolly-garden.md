# Plano: Resolver erros restantes em lint-fixes-no-hook

## Estado Atual

**Branch:** `lint-fixes-no-hook` (11 commits à frente de `main`, 369 arquivos alterados)

| Ferramenta | Erros |
|:--|:--|
| ESLint | **0** — limpo |
| TypeScript | **4** — 1 arquivo |
| Vitest | **2 testes** — 2 arquivos |

**stash@{1}:** Todos os 360 arquivos já estão na branch. Pode ser ignorado.

---

## Erro 1: TypeScript — `jira_management/main.test.ts:552,575`

`fs.readdirSync` sem `{ encoding }` retorna `Dirent[]`. O mock retorna `string[]` → TS2322.

**Correção:** Adicionar typed helper após os imports (linha ~134):
```ts
const mockReaddirSync = fs.readdirSync as unknown as ReturnType<typeof vi.fn<() => string[]>>;
```
Substituir `vi.mocked(fs.readdirSync)` por `mockReaddirSync` nas linhas 552, 566, 575.

---

## Erro 2: Teste — `scripts/audit/structural.test.ts:27`

O spy em `console.log` captura output do `rootLogger` antes do JSON. Falta mock do logger.

**Correção:** Adicionar mock do logger:
```ts
vi.mock('../../shared/logger.js', () => ({
    rootLogger: { warn: vi.fn(), info: vi.fn(), debug: vi.fn() },
}));
```

---

## Erro 3: Teste — `shared/__tests__/integration/git-metrics-adapter.integration.test.ts:97-98`

Commits datados "2026-05-01" e "2026-06-15" com `maxDays: 7`. Hoje é 2026-06-24, cutoff = 2026-06-17. Ambos filtrados → resultado 0, esperado 1.

**Correção:** Usar data dinâmica para o commit recente:
```ts
commit('old commit', '2026-05-01T10:00:00');
const recentDate = new Date(Date.now() - 2 * 24 * 60 * 60 * 1000).toISOString();
commit('recent commit', recentDate);
```

---

## Ordem de Execução

1. Ler `jira_management/main.test.ts` (contexto completo)
2. Corrigir TS errors (helper + 3 substitutions)
3. Corrigir `structural.test.ts` (mock logger)
4. Corrigir `git-metrics-adapter.integration.test.ts` (data dinâmica)
5. `npx tsc --noEmit` → 0 erros
6. `npx vitest run` → 0 falhas
7. Commit

## Verificação Final

```bash
npx tsc --noEmit    # 0 erros
npx vitest run      # 0 falhas
npx eslint src/     # 0 (já limpo)
```
