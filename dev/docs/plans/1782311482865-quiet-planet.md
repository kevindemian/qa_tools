# Plano: Retomada de Correção de Erros (lint, tsc, testes)

## Estado Atual

| Métrica | Valor |
|---------|-------|
| Branch | `lint-fixes-no-hook` (11 commits ahead de `main`) |
| Arquivos alterados (committed) | 369 (+7,245 / -2,201) |
| Arquivos modificados (unstaged) | 8 (todos `.test.ts`) |
| Erros TypeScript | 33 (1 arquivo corrompido) |
| Violações ESLint | 4.660 |
| Testes falhando | 3 testes em 4 arquivos |

---

## Erros Críticos Imediatos

### 1. Arquivo corrompido: `git_triggers/interactive-mode.test.ts`
- **Causa raiz**: `expect(true).toBe(true)` inserido dentro de um array literal (~linha 780)
- **Efeito**: 33 erros de parse em cascata, 0 testes executados
- **Ação**: Remover a linha estranha, restaurar a estrutura do array

### 2. Arquivos com mudanças unstaged (8 arquivos)
Precisa decidir: commit ou discard?
- `git_triggers/interactive-mode.test.ts`
- `git_triggers/main.test.ts`
- `jira_management/commands/handlers.test.ts`
- `jira_management/commands/test-execution-flow.test.ts`
- `jira_management/cypress_resource.test.ts`
- `shared/http-client.test.ts`
- `shared/logger.test.ts`
- `shared/quality-metrics.test.ts`

### 3. Violações ESLint: 4.660
Regras mais frequentes:
- `vitest/expect-expect` — testes sem `expect.assertions()` ou asserts significativos
- `vitest/prefer-called-with` — **NÃO auto-fixar** (regra do usuário: converte `toHaveBeenCalled()` sem args, quebra 200+ testes)
- `security/detect-non-literal-fs-filename` — writeFileSync com path não-literal
- `vitest/prefer-strict-equal` — `toEqual()` vs `toStrictEqual()`
- `vitest/prefer-hooks-in-order` — ordenação de hooks

### 4. Testes falhando (3 testes reais)
| Arquivo | Erro | Causa |
|---------|------|-------|
| `scripts/audit/structural.test.ts` | SyntaxError JSON.parse | Output contém texto de env-loader (emoji) |
| `shared/__tests__/temp-dir.property.test.ts` | EISDIR | `writeReport(".", "")` tenta abrir diretório |
| `shared/__tests__/integration/git-metrics-adapter.integration.test.ts` | Expected length 1, got 0 | Filtro `maxDays` depende de datas do git |

---

## Regras Obligatórias (AGENTS.md + Durable Knowledge)

1. **ZERO suppressão/bypass** — sem `eslint-disable`, `.skip`, workarounds
2. **`prefer-called-with` NUNCA auto-fixar** — reverter após cada batch de `--fix`
3. **Subagentes proibidos para lint trabalho** — processamento sequencial por arquivo
4. **`expect.hasAssertions()` proibido** — usar asserts significativos
5. **`valid-title` rodar DEPOIS de `prefer-lowercase-title`** — ordem importa
6. **Red-Green-Refactor obrigatório** — teste primeiro, código depois
7. **Expectations de teste imutáveis** — nunca alterar valores esperados existentes
8. **SOP Protocol** — ler seção do SOP antes de cada Phase

---

## Plano de Execução — Sessão Atual (Erros Críticos)

Escopo: Fases 1-2-4 (erros bloqueantes). Lint (4.660 violações) fica para sessão dedicada.

### Fase 1: Corrigir Arquivo Corrompido
1. Ler `git_triggers/interactive-mode.test.ts` ~linhas 775-790
2. Remover `expect(true).toBe(true)` da linha 780
3. Verificar se o array `tests: [...]` está íntegro
4. Rodar `npx tsc --noEmit` → esperado: 0 erros TS

### Fase 2: Avaliar e Decidir Sobre 8 Arquivos Unstaged
**Abordagem: avaliar um por um** (mais segura que commit/discard cego)

1. Ler diff de cada arquivo unstaged
2. Classificar cada um:
   - ✅ Fix de lint válido → commitar
   - ⚠️ Código corrompido/incompleto → descartar
   - ❓ Incerto → documentar e perguntar ao usuário
3. Commitar fixes válidos em batch separado
4. Descartar corruption/incompleto com `git checkout -- <file>`

**Critérios de classificação:**
- Fix válido: mudança cosmética (capitalização, espaçamento, import ordenado)
- Corrupção: `expect(true).toBe(true)` solto, código truncado, sintaxe quebrada
- Incerto: mudança lógica que pode alterar comportamento do teste

### Fase 3: Correções de Testes (3 falhas reais)
1. `scripts/audit/structural.test.ts` — filtrar output de env-loader antes do JSON.parse
2. `shared/__tests__/temp-dir.property.test.ts` — adicionar validação de path (não diretório)
3. `shared/__tests__/integration/git-metrics-adapter.integration.test.ts` — ajustar filtro de datas para não depender de estado do git

### Fase 4: Validação e Commit
1. `npx tsc --noEmit` → 0 erros
2. `npx vitest run` → todos os testes passando (ou falhas conhecidas documentadas)
3. `git status` → working tree limpo ou com apenas mudanças intencionais
4. Commit com mensagem descritiva

---

## Sessões Futuras (Lint Completo)

### Fase L1: Fix Manual Sequencial (por arquivo)
Seguindo regra do usuário: processamento sequencial, sem subagentes.

**Ordem de prioridade:**
1. Arquivos com erros de compilação/parse primeiro
2. Arquivos com testes falhando
3. Demais arquivos (ordem alfabética por diretório)

**Regras de fix por tipo de violação:**

| Regra | Ação | Notas |
|-------|------|-------|
| `vitest/expect-expect` | Adicionar asserts significativos | NUNCA usar `expect.hasAssertions()` |
| `vitest/prefer-called-with` | **NÃO ALTERAR** | Manter `toHaveBeenCalled()` sem args |
| `security/detect-non-literal-fs-filename` | Usar path literal ou variável tipada | Não suprimir |
| `vitest/prefer-strict-equal` | Trocar `toEqual()` por `toStrictEqual()` | Verificar compatibilidade |
| `vitest/prefer-hooks-in-order` | Reordenar hooks no arquivo | `beforeAll` antes de `beforeEach` |
| `vitest/require-to-throw-message` | Usar `toThrow(/error/i)` | NÃO usar string message |

**Ordem de execução:** `prefer-lowercase-title` ANTES de `valid-title`

### Fase L2: Validação Final Lint
1. `npx tsx scripts/quality-check.ts` → 0 violações
2. `npx vitest run` → regressão zero
3. Commit final

---

## Verificação

Comandos de validação ao final:
```bash
npx tsc --noEmit                    # 0 erros TypeScript
npx tsx scripts/quality-check.ts    # 0 violações lint
npx vitest run                      # Todos os testes passando
git status                          # Working tree limpo
git log --oneline -5                # Histórico de commits
```

---

## Riscos e Limitações

1. **4.660 violações de lint** — número alto; pode exigir múltiplas sessões
2. **Regra `prefer-called-with`** — requer atenção redobrada a cada batch de fix
3. **Testes datados** — `git-metrics-adapter` pode ser sensível ao momento de execução
4. **Processamento sequencial** — será lento mas é obrigatório pela regra do usuário
5. **Ordem de fixes** — `valid-title` DEVE vir depois de `prefer-lowercase-title`
