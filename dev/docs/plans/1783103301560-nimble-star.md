# Avaliação de Retomada: CiDataHub — Gaps Identificados

## Resumo

Avaliamos as 11 condições do usuário contra o estado real do código + BACKLOG.md. O Sprint CiDataHub Resumption marcou CDH-R1 a CDH-R8 como ✅, mas verificação independente identificou **8 gaps genuínos**. GC-07 (metrics.ts usando Store) está completo (condition #11 é falsa).

## Análise por Condição

| # | Condição | Veredicto | Evidência |
|---|----------|-----------|-----------|
| 1 | Backlog updates R1-R8 | ✅ OK | BACKLOG.md:10-94 documenta R1-R8 com status ✅ |
| 2 | 100% test coverage verificado | ❌ GAP | `vitest.config.ts:12-28` tem thresholds configurados, mas `vitest run --coverage` nunca foi executado. Métricas R8 dizem "5819 pass" mas não reportam coverage % |
| 3 | Auditoria completa 21 consumidores | ❌ GAP | R7a diz "5 consumers" (BACKLOG:66). Plano original lista 21 features, mas só 6 arquivos de produção importam MetricsStore/MetricsRun. 5 aceitam ciData. 1 (run-comparison.ts) não. Auditoria deveria documentar por que run-comparison.ts é N/A |
| 4 | Menu integration testada | ❌ GAP | CDH-R4b (BACKLOG:47) marca "✅ (TSC valida existência)", mas `ci-menu.integration.test.ts` NÃO EXISTE no disco. TSC valida tipos, não funcionalidade |
| 5 | E2E com API real | ❌ GAP | `ci-data-e2e.test.ts` usa `vi.fn()` (lines 22-45). Nenhuma chamada API real |
| 6 | Documentação código completo | ❌ GAP | `_showCiDataHubSummary()` em `interactive-mode.ts:601` sem JSDoc. Sem outros comentários nos testes novos |
| 7 | Fase 16 executada (deprecated) | ❌ GAP | `metrics.ts` linhas 10 e 36: zero `@deprecated`. Nenhum `@deprecated` em MetricsRun/MetricsStore |
| 8 | Sem empty catches | ⚠️ PARCIAL | `ci-data.ts:136` — catch com só comentário, sem logging. Catch em 195 tem warn+return OK |
| 9 | D5.5/D6.4 resolvido | ❌ GAP | CDH-R6a (BACKLOG:59) diz ✅, mas outlier treatment SÓ em avgDuration (ceiling 86400s) e suiteSpeedP95 (P95). D6.4: logger.info existe mas nenhum test valida progress display |
| 10 | Fase 16 nunca executada | ❌ = #7 | Confirmado: zero @deprecated |
| 11 | GC-07 nunca endereçado | ✅ FALSO | `metrics.ts:6-7` importa StoreBackend. `loadMetrics()` (line 123) e `saveMetrics()` (line 136) usam StoreBackend. GC-07 está completo |

---

## Gaps Detalhados

### GAP-1: Coverage nunca verificado
- `vitest.config.ts` tem config: v8 provider, thresholds lines≥90, functions≥91, branches≥80, statements≥90
- `package.json:13` tem script `"test:coverage"`
- NUNCA foi executado para confirmar que passa

### GAP-2: Auditoria de consumidores incompleta
- 6 arquivos de produção importam MetricsStore/MetricsRun:
  1. health-score.ts → aceita ciData ✅
  2. quality-gate.ts → aceita ciData ✅
  3. pr-report-core.ts → aceita ciData ✅
  4. pipeline-cost.ts → aceita ciData ✅
  5. traceability-matrix.ts → aceita ciData ✅
  6. run-comparison.ts → NÃO aceita ciData (usa MetricsRun como input LLM, não fonte de dados)
- BACKLOG:66 diz "5 consumers" mas deveria justificar por que run-comparison.ts é N/A

### GAP-3: Menu integration test ausente
- `shared/__tests__/integration/ci-menu.integration.test.ts` NÃO EXISTE
- BACKLOG:47 marca CDH-R4b como "TSC valida existência" — insuficiente
- Menu option `h` existe em `interactive-mode.ts:707-710`
- Handler `_showCiDataHubSummary` em `interactive-mode.ts:601-654`

### GAP-4: E2E tests usam mock
- `ci-data-e2e.test.ts:22-45`: `createMockProvider(runs, jobsPerRun)` com `vi.fn()`
- Decisão do usuário: Ambos — manter mocks + criar testes condicionais com `GITHUB_TOKEN`/`GITLAB_TOKEN`

### GAP-5: Documentação menu handler
- `_showCiDataHubSummary()` em `interactive-mode.ts:601` sem JSDoc

### GAP-6: @deprecated não aplicado
- `MetricsRun` (metrics.ts:10) e `MetricsStore` (metrics.ts:36) sem `@deprecated`
- Plano Fase 16 previa: "Marcar MetricsRun e MetricsStore com @deprecated quando CI disponível"

### GAP-7: Empty catch em ci-data.ts
- `ci-data.ts:136`: `} catch {` com só `// Artifacts são opcionais` — sem logging
- Demais catches têm debug/warn logging

### GAP-8: D5.5 outlier treatment incompleto
- avgDuration: ceiling 86400s ✅
- suiteSpeedP95: P95 ignora top 5% ✅
- passRate: percentage 0-100, não precisa
- topFailingJobs: top-10, já filtrado
- branchBreakdown: percentage por branch
- flakyTests: rate 0-100
- costEstimate: minutos reais

---

## Plano de Correção

### Fase A — Verificação de Coverage (GAP-1)
1. Executar `vitest run --coverage` e registrar resultado
2. Se thresholds não atingidos, criar testes adicionais
3. Documentar resultado no BACKLOG.md

### Fase B — Auditoria de Consumidores (GAP-2)
1. Justificar por que run-comparison.ts é N/A (usa MetricsRun como input LLM, não fonte CI)
2. Documentar no BACKLOG.md com a tabela completa de 6 arquivos avaliados

### Fase C — Menu Integration Test (GAP-3)
1. Criar `shared/__tests__/integration/ci-menu.integration.test.ts`
2. Validar opção `h` existe no menu
3. Mockar createCiDataHub, verificar handler chamado
4. Validar output formatado

### Fase D — E2E Live Tests (GAP-4)
**Decisão:** Ambos — mocks como baseline + condicionais com token
1. Criar `shared/__tests__/e2e/ci-data-e2e-live.test.ts`
2. `describe.skipIf(!process.env.GITHUB_TOKEN && !process.env.GITLAB_TOKEN)`
3. Testar: fetch runs, create hub, compute metrics com API real
4. Documentar no BACKLOG.md

### Fase E — Documentação (GAP-5)
1. JSDoc em `_showCiDataHubSummary()` em `interactive-mode.ts:601`
2. Verificar consistência com outros handlers do menu

### Fase F — @deprecated (GAP-6)
1. `MetricsRun` (metrics.ts:10): `/** @deprecated Use CiDataHub when CI is available. Kept for local/dev fallback. */`
2. `MetricsStore` (metrics.ts:36): `/** @deprecated Use CiDataHub when CI is available. Kept for local/dev fallback. */`

### Fase G — Empty Catch (GAP-7)
1. `ci-data.ts:136`: adicionar `rootLogger.debug('CiDataHub: artifacts não disponíveis para run ${runIdNum}')`

### Fase H — D5.5 Justificativa (GAP-8)
1. Documentar por que passRate, topFailingJobs, branchBreakdown, flakyTests, costEstimate não precisam de outlier treatment
2. Atualizar BACKLOG.md

### Fase I — Atualização do BACKLOG
1. Corrigir status CDH-R4b: "arquivo não existe, criado nesta sprint"
2. Corrigir status CDH-R6a: "outlier treatment em avgDuration + suiteSpeedP95, justificativa para demais"
3. Atualizar métricas R8 com resultado do coverage

---

## Ordem de Execução

```
Fase A (coverage) → Fase B (auditoria) → Fase C (menu test) → 
Fase D (e2e live) → Fase E (docs) → Fase F (@deprecated) → 
Fase G (empty catch) → Fase H (D5.5 justificativa) → 
Fase I (backlog update) → Validação final (tsc + vitest + lint)
```

## Validação Final

| Comando | Critério |
|---------|----------|
| `npx tsc --noEmit` | 0 erros |
| `npx vitest run` | 100% pass |
| `npx vitest run --coverage` | Thresholds atingidos |
| `npm run lint` | 0 violações |
| `ci-menu.integration.test.ts` | Criado e passando |
| `ci-data-e2e-live.test.ts` | Criado (skipIf sem token) |

## Decisões Registradas

- **GAP-4 (E2E):** Usuário escolheu "Ambos: mock + condicional"
- **GAP-8 (D5.5):** PassRate/topFailingJobs/flakyTests/costEstimate não precisam de outlier treatment — justificar no BACKLOG
