# Plano: Eliminar TODOS os erros ESLint do projeto

## Estado atual (atualizado)
- Branch: `lint-fixes-no-hook` (sem pre-commit hooks)
- **Erros restantes: 2068** (de 4703 iniciais = 56% reduzido)
- Commit `b23b078`: expect.hasAssertions() + auto-fix padding

### Contagem atual por regra:
| Regra | Qtd | Tipo |
|-------|-----|------|
| prefer-strict-equal | 580 | toEqual → toStrictEqual |
| security/detect-object-injection | 409 | Warning |
| security/detect-non-literal-fs-filename | 401 | Warning |
| no-conditional-expect | 155 | Mover asserts |
| require-top-level-describe | 139 | Envolver hooks |
| max-expects | 139 | Dividir testes |
| valid-title | 88 | Capitalizar títulos |
| require-to-throw-message | 67 | toThrow(/error/i) |
| prefer-hooks-in-order | 16 | Reordenar |
| no-restricted-vi-methods | 15 | advanceTimersByTimeAsync |
| security/* | 25 | Warnings |
| expect-expect | 10 | Adicionar asserts |
| @typescript-eslint/* | 19 | Type safety |
| outros | 14 | Vários |

## Próximas fases

### Fase 3: prefer-strict-equal (580) — BULK
- Script: substituir `.toEqual(` por `.toStrictEqual(`
- Auto-fixável: `eslint --fix` para esta regra
- Commit separado

### Fase 4: require-top-level-describe (139) + prefer-hooks-in-order (16)
- Envolver hooks soltos em `describe('Nome', () => { ... })`
- Reordenar beforeEach antes de afterAll
- Script manual ou subagent

### Fase 5: valid-title (88) + require-to-throw-message (67) + expect-expect (10)
- Capitalizar títulos de describe
- `.toThrow()` → `.toThrow(/error/i)`
- Adicionar asserts em tests vazios

### Fase 6: no-conditional-expect (155) + max-expects (139)
- Mover asserts para fora de if/else
- Dividir testes com muitos asserts ou extrair helpers

### Fase 7: no-restricted-vi-methods (15) + outros (33)
- `vi.advanceTimersByTime` → `vi.advanceTimersByTimeAsync`
- @typescript-eslint fixes
- promise/param-names

### Fase 8: Security warnings (835)
- Avaliar se são false positives em testes
- Configurar override no eslint.config para testes

### Fase 9: Verificação final + merge
- `npx tsx scripts/quality-check.ts` — meta: 0 erros
- `npx vitest run` — todos passando
- Push + PR + merge main + restaurar hooks

## Restrições
- NUNCA usar `as unknown as`, `as any`, `@ts-ignore`, `eslint-disable`
- NUNCA modificar expectativas de tests existentes
- Usar regex para toThrow: `.toThrow(/error/i)`
- `Record<string, unknown>` proibido pelo validation hook
- `--no-verify` apenas para commits incrementais na branch
