# SSOT Enforcement — Corrective Plan & Implementation Resumption

> **Criado:** 2026-07-11
> **Baseado em:** Avaliação profunda do plano `data-hub-ssot-enforcement.md` via 3 explore subagents
> **Objetivo:** Corrigir gaps encontrados e retomar implementação do plano SSOT

---

## 1. INVENTÁRIO COMPLETO DE GAPS

### 1.1 Gaps Críticos (Bloqueiam Fases Posteriores)

| ID | Fase | Gap | Arquivo | Linha | Descrição | Bloqueia |
|----|------|-----|---------|-------|-----------|----------|
| G1 | 0.2.2 | Silent error residual | `shared/data-hub/providers/github-provider.ts` | 262 | `String(err)` não substituído por `extractErrorMessage(err)` | Fase 7 auditoria |
| G2 | 0.2.2 | Silent error residual | `git_triggers/gitlab-workflow.ts` | 283 | bare `catch {` silencia erros | Fase 7 auditoria |
| G3 | 0.8 | Interface incompleta | `shared/types/data-hub.ts` | — | `loadMetricsStore()` não está na interface pública DataHub (apenas em DataHubPersistence interna) | Fase 8 (deletar sources) |
| G4 | 0.8 | Export não removido | `shared/data-hub/persistence.ts` | 67 | `createDataHubPersistence` ainda exportado publicamente | Fase 8 (Tarefa 0.8.10) |
| G5 | 1 | Dimension 5 incompleto | `shared/release-score.ts` | — | Zero provenance de pesos/thresholds | Compliance |
| G6 | 1 | Dimension 5 incompleto | `shared/requirement-score.ts` | — | Zero provenance de pesos/thresholds | Compliance |
| G7 | 1 | Dimension 5 incompleto | `shared/backlog-health.ts` | — | Zero provenance de pesos/thresholds | Compliance |
| G8 | 1 | Dimension 5 incompleto | `shared/silent-regression.ts` | — | Zero provenance de z-score thresholds | Compliance |
| G9 | 1 | Dimension 5 incompleto | `shared/quality-metrics.ts` | — | Zero provenance de drift threshold | Compliance |
| G10 | 1 | Dimension 5 incompleto | `shared/cross-squad-benchmark.ts` | — | Zero provenance de metodologia | Compliance |
| G11 | 1 | Dimension 5 incompleto | `shared/impact-alert.ts` | — | Zero provenance de alert thresholds | Compliance |
| G12 | 8 | Módulo não deletado | `shared/ci-test-downloader.ts` | — | Ainda existe (149 linhas), consumidor ativo em session-context.ts:21 | SSOT |
| G13 | 8 | Módulo não deletado | `shared/coverage-source.ts` | — | Ainda existe (94 linhas) | SSOT |
| G14 | 8 | Módulo não deletado | `shared/commit-log.ts` | — | Ainda existe (114 linhas), consumidores ativos em case17.ts:31 e providers | SSOT |
| G15 | 10 | ESLint sem enforcement | `eslint.config.mjs` | — | Nenhuma regra bloqueia `loadMetricsStore` fora de `shared/data-hub/` | Prevenção |
| G16 | 11 | Reporter detection não iniciado | — | — | Fase 11 (AST/híbrido) não implementada | Funcionalidade |

### 1.2 Gaps Menores (Não bloqueantes)

| ID | Fase | Gap | Descrição |
|----|------|-----|-----------|
| G17 | 3 | parseTestResultsFile residual | `jira_management/commands/case17.ts:326` ainda usa parseTestResultsFile |
| G18 | 7 | Auditoria pós-migração não verificada | Phase 7 (Tarefa 7.1) não executada — requer suite completa |

---

## 2. ANÁLISE DE DEPENDÊNCIAS

```
G1,G2 (silent errors) ──────────────────────────────────┐
G3,G4 (interface/export) ────────────────────────────────┤
                                                         ▼
G12,G13,G14 (deletar módulos) ←─ precisa de G3,G4 ──── Fase 8
                                                         │
G5-G11 (Dimension 5) ←─ independentes ──────────────────┤
                                                         │
G15 (ESLint) ←─ precisa de Fase 8 completa ─────────────┤
                                                         │
G16 (reporter detection) ←─ independente ────────────────┤
                                                         ▼
G18 (auditoria final) ←─ precisa de TODOS acima ─────── Fase 7
```

### Sequência Corrigida

```
CORREÇÃO Imediata (G1, G2)
    → Corrigir 2 silent errors restantes
    → Commit

CORREÇÃO Interface (G3, G4)
    → Adicionar loadMetricsStore à interface DataHub (ou remover daqui se Fase 1 já remove)
    → Remover export createDataHubPersistence
    → Commit

Dimension 5 (G5-G11) — PARALELIZÁVEL
    → 7 módulos independentes, podem ser feitos em batch
    → Commit por módulo ou batch

Fase 8 — Deletar Módulos (G12-G14)
    → PREREQUISITO: migrar consumidores restantes
    → session-context.ts: migrar de ci-test-downloader para DataHub
    → case17.ts: migrar de commit-log para DataHub providers
    → coverage-source.ts: verificar consumidores, migrar se necessário
    → Deletar os 3 módulos
    → Commit

Fase 10 — ESLint Enforcement (G15)
    → PREREQUISITO: Fase 8 completa
    → Adicionar regra no-restricted-syntax para loadMetricsStore
    → Commit

Fase 11 — Reporter Detection (G16)
    → Pesquisa de viabilidade (AST, package.json, regex)
    → Implementação baseada em pesquisa
    → Commit

Fase 7 — Auditoria Final (G18)
    → Executar TODOS os comandos de verificação
    → Commit
```

---

## 3. PLANO DE CORREÇÃO DETALHADO

### 3.1 CORREÇÃO Imediata: Silent Errors (G1, G2)

**Tarefa C1: Corrigir github-provider.ts:262**
- Arquivo: `shared/data-hub/providers/github-provider.ts`
- Linha 262: `String(err)` → `extractErrorMessage(err)` (já importado no arquivo)
- Verificar: `rg "String\(err\)" shared/data-hub/providers/` = 0 resultados

**Tarefa C2: Corrigir gitlab-workflow.ts:283**
- Arquivo: `git_triggers/gitlab-workflow.ts`
- Linha 283: bare `catch {` → `catch (err: unknown) { rootLogger.warn(extractErrorMessage(err)); ... }`
- Verificar: `rg "catch \{" git_triggers/gitlab-workflow.ts` = 0 resultados

**Checkpoint C1-C2:**
```bash
npx tsc --noEmit
rg "String\(err\)" shared/data-hub/providers/  # 0
rg "catch \{" git_triggers/gitlab-workflow.ts   # 0
npx vitest run shared/data-hub/ git_triggers/   # 100% pass
```

---

### 3.2 CORREÇÃO Interface: DataHub (G3, G4)

**Tarefa C3: Decidir loadMetricsStore na interface**
- Opção A: Adicionar `loadMetricsStore()` à interface DataHub (plano Fase 0.8)
- Opção B: Pular — Fase 1.3 já remove `loadMetricsStore` dos consumidores
- **Recomendação:** Opção B — se nenhum consumidor externo precisa, não adicionar à interface. A ausência é mais forte que a presença + remoção posterior.

**Tarefa C4: Remover export createDataHubPersistence**
- Arquivo: `shared/data-hub/persistence.ts`
- Linha 67: remover `export` de `createDataHubPersistence`
- `factory.ts:55` importa via dynamic import — verificar se funciona sem export público
- Verificar: `rg "createDataHubPersistence" --include='*.ts' -g '!__tests__' -g '!*.test.ts'` = 0 em produção fora de data-hub/

**Checkpoint C3-C4:**
```bash
npx tsc --noEmit
rg "createDataHubPersistence" --include='*.ts' -g '!__tests__' -g '!*.test.ts' -g '!shared/data-hub/**'  # 0
```

---

### 3.3 Dimension 5: Provenance (G5-G11)

**Tarefa C5-C11: Adicionar provenance a 7 módulos**

| Tarefa | Arquivo | Provenance | Referências |
|--------|---------|------------|-------------|
| C5 | `shared/release-score.ts` | RELEASE_SCORE_PROVENANCE | ISO/IEC 25023:2016, DORA |
| C6 | `shared/requirement-score.ts` | REQUIREMENT_SCORE_PROVENANCE | Internal best practice |
| C7 | `shared/backlog-health.ts` | BACKLOG_HEALTH_PROVENANCE | Internal best practice |
| C8 | `shared/silent-regression.ts` | SILENT_REGRESSION_PROVENANCE | ISO 3534-2 |
| C9 | `shared/quality-metrics.ts` | DRIFT_DETECTION_PROVENANCE | ISO 3534-2 |
| C10 | `shared/cross-squad-benchmark.ts` | BENCHMARK_PROVENANCE | DORA / Internal |
| C11 | `shared/impact-alert.ts` | IMPACT_ALERT_PROVENANCE | Internal best practice |

**Padrão para cada módulo:**
1. Adicionar const `XXX_PROVENANCE` com pesos, thresholds, fontes, padrões
2. Adicionar JSDoc com referências normativas
3. Validar pesos com `Number.isFinite` + soma = 1.0 (onde aplicável)
4. Adicionar testes PBT para validação de invariantes

**Checkpoint C5-C11:**
```bash
rg "PROVENANCE" shared/release-score.ts shared/requirement-score.ts shared/backlog-health.ts shared/silent-regression.ts shared/quality-metrics.ts shared/cross-squad-benchmark.ts shared/impact-alert.ts  # >= 7 ocorrências
npx vitest run shared/__tests__/dimension5*  # 100% pass
```

---

### 3.4 Fase 8: Deletar Módulos (G12-G14)

**Pré-requisito:** Migrar consumidores restantes

**Tarefa C12: Migrar session-context.ts de ci-test-downloader**
- Arquivo: `shared/session-context.ts`
- Linha 21: import de `fetchLatestTestRun` de `ci-test-downloader.js`
- Ação: substituir por chamada DataHub (`hub.raw.parsedArtifacts` ou provider interno)
- Verificar que `resolveTestDataSource()` continua funcionando

**Tarefa C13: Migrar case17.ts de commit-log**
- Arquivo: `jira_management/commands/case17.ts`
- Linha 31: import de `fetchCommitLog` de `commit-log.js`
- Ação: substituir por DataHub provider (`buildCommitLog` via data-hub/providers)
- Verificar que case17 continua compilando e testes passam

**Tarefa C14: Verificar e migrar consumidores de coverage-source**
- `rg "coverage-source" --include='*.ts' -g '!__tests__'` — mapear todos os consumidores
- Migrar cada um para DataHub
- Se zero consumidores restantes, prosseguir para deleção

**Tarefa C15: Deletar módulos**
- Deletar `shared/ci-test-downloader.ts` + teste
- Deletar `shared/coverage-source.ts` + teste + property test + integration test
- Deletar `shared/commit-log.ts` + teste

**Checkpoint C12-C15:**
```bash
npx tsc --noEmit
rg "ci-test-downloader|coverage-source|commit-log" --include='*.ts' -g '!__tests__' -g '!*.test.ts' -g '!shared/data-hub/**' -g '!docs' -g '!plans'  # 0
npx vitest run --reporter=verbose | tail -10  # 100% pass
```

---

### 3.5 Fase 10: ESLint Enforcement (G15)

**Tarefa C16: Adicionar regra SSOT**
- Arquivo: `eslint.config.mjs`
- Adicionar `no-restricted-syntax` para bloquear `loadMetricsStore` fora de `shared/data-hub/`
- Ou usar `no-restricted-imports` para bloquear imports de módulos deletados

**Checkpoint C16:**
```bash
npx eslint . --max-warnings=0  # 0 violações
```

---

### 3.6 Fase 11: Reporter Detection (G16)

**Tarefa C17: Pesquisa de viabilidade**
- Escopo: AST parsing (ts-morph, jscodeshift), package.json check, regex expandida
- Entregável: documento de decisão
- Checkpoint: decisão documentada

**Tarefa C18: Implementação**
- Baseado na pesquisa da C17
- Frameworks: vitest, jest, cypress, playwright, generic
- Formatos: CTRF, JUnit, Mochawesome

**Checkpoint C17-C18:**
```bash
npx tsc --noEmit
npx vitest run setup/  # 0 falhas
```

---

### 3.7 Fase 7: Auditoria Final (G18)

**Tarefa C19: Executar auditoria completa**
- Executar TODOS os comandos da seção "Checkpoint Final da Fase 1" do plano original
- Executar verificação de Phase 7 (Tarefa 7.1)
- Registrar resultados em PROGRESS.md

**Checkpoint C19:**
```bash
# TODOS os comandos do plano original devem passar
npx tsc --noEmit
npx eslint . --max-warnings=0
npx vitest run --reporter=verbose | tail -10
rg "loadMetricsStore" --include='*.ts' -g '!__tests__' -g '!*.test.ts' -g '!shared/data-hub/**'  # 0
rg "store\.runs" --include='*.ts' -g '!__tests__' -g '!*.test.ts' -g '!shared/data-hub/**'  # 0
rg "dataHub\?: DataHub" shared/health-score.ts shared/quality-gate.ts shared/pr-report-core.ts  # 0
```

---

## 4. RESUMO EXECUTIVO

### Estado Atual
- **Fases completas (100%):** 0.1, 0.2, 0.2.1, 0.3, 0.5, 0.6, 0.7, 1 (core SSOT), 3, 4, 5, 6
- **Fases parciais:** 0.2.2 (2 gaps), 0.8 (2 gaps), 1 (Dimension 5)
- **Fases não iniciadas:** 7 (verificação), 8, 9, 10, 11

### Prioridades
1. **IMEDIATO:** G1, G2 (2 silent errors — 5 min cada)
2. **CURTO PRAZO:** G3, G4 (interface — decisões rápidas)
3. **MÉDIO PRAZO:** G5-G11 (Dimension 5 — batch paralelizável)
4. **MÉDIO PRAZO:** G12-G14 (deletar módulos — requer migração prévia)
5. **Longo PRAZO:** G15 (ESLint), G16 (reporter detection)
6. **FINAL:** G18 (auditoria completa)

### Esforço Estimado
| Fase | Tarefas | Esforço |
|------|---------|---------|
| C1-C2 (silent errors) | 2 | ~15 min |
| C3-C4 (interface) | 2 | ~30 min |
| C5-C11 (Dimension 5) | 7 | ~2-3h |
| C12-C15 (deletar módulos) | 4 | ~2-3h |
| C16 (ESLint) | 1 | ~30 min |
| C17-C18 (reporter detection) | 2 | ~4-6h |
| C19 (auditoria final) | 1 | ~1h |
| **Total** | **19** | **~10-14h** |
