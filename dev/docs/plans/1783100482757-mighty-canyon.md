# Retomada: CiDataHub — Correção de 6 Gaps + Validação Completa

## Contexto

O plano original (1783070272726-lucky-sailor.md) marcou CDH-0 a CDH-18 como ✅, mas auditoria do usuário identificou 6 gaps que impedem considerar a tarefa completa. A retomada deve corrigir TODOS os gaps e validar que a funcionalidade está completa de ponta a ponta.

## Gaps Identificados

| # | Gap | Evidência | Severidade |
|---|-----|-----------|------------|
| 1 | Fase 18 (auditoria pós-implementação) não realizada | Apenas testes básicos rodados, não a auditoria completa descrita no plano | Alta |
| 2 | Testes de sistema e e2e ausentes | Apenas 10 unit + 8 integration tests para CiDataHub. Zero para fluxo completo | Alta |
| 3 | Integração com menu não verificada | Nenhuma verificação de que CiDataHub aparece no menu correto | Média |
| 4 | Execução end-to-end não verificada | Nenhum e2e test criado ou rodado | Alta |
| 5 | TECHDOC.md não atualizado | Grep retorna 0 matches para CiDataHub/CI Data Hub | Média |
| 6 | D5.5/D6.4 conformity gaps | D5.5 outlier treatment parcial (saturação só em avgDuration), D6.4 progress feedback nunca corrigido | Média |

## Plano de Retomada

### Fase R1 — Corrigir CDH-15 (Backlog vs Realidade)

**Problema:** BACKLOG.md marca CDH-18 como ✅ mas os testes de consumidores NÃO foram atualizados com cenários CiDataHub.

**Ação:**
- Verificar quais testes de integração de consumidores existem
- Adicionar cenários `ciData` em: health-score, quality-gate, pr-report-core, pipeline-cost, traceability-matrix, flakiness-dashboard, defect-trend, quarantine, run-comparison
- Cada teste deve: criar CiDataHub mock, passar para consumer, verificar que dados do hub são usados (não MetricsStore)

**Arquivos:**
- `shared/__tests__/integration/health-score.integration.test.ts`
- `shared/__tests__/integration/quality-gate.integration.test.ts`
- `shared/__tests__/integration/pr-report-core.integration.test.ts`
- `shared/__tests__/integration/pipeline-cost.integration.test.ts`
- `shared/__tests__/integration/traceability-matrix.integration.test.ts`
- `shared/__tests__/integration/flakiness-dashboard.integration.test.ts`
- `shared/__tests__/integration/defect-trend.integration.test.ts`
- `shared/__tests__/integration/quarantine.integration.test.ts`
- `shared/__tests__/integration/run-comparison.integration.test.ts`

### Fase R2 — Testes de Sistema (System Tests)

**Problema:** Usuário exige "tenha especial atenção aos testes de integração, sistema e e2e". Apenas unit + integration existem.

**Ação:**
- Criar `shared/__tests__/system/ci-data-system.test.ts`
- Testar fluxo completo: GitProvider mock → createCiDataHub → health-score → quality-gate → PR report output
- Verificar que os dados do hub propagam corretamente por TODA a cadeia de consumidores
- Testar cenários: dados completos, dados parciais, provider falha, fallback para MetricsStore

### Fase R3 — Testes E2E (End-to-End)

**Problema:** Nenhum e2e test verifica que CiDataHub funciona do início ao fim.

**Ação:**
- Criar `shared/__tests__/e2e/ci-data-e2e.test.ts`
- Simular pipeline completo: GitHub API response → CiDataHub → consumer chain → output HTML/JSON
- Verificar que o report HTML final contém dados do CI (não zeros/mock)
- Verificar que quality gate usa dados reais do CI

### Fase R4 — Verificação de Integração com Menu

**Problema:** Usuário exige verificar "se a funcionalidade está corretamente apresentada no menu".

**Ação:**
- Verificar como CiDataHub é acessível via menu (git_triggers/interactive-mode.ts ou setup/main.ts)
- Se não está no menu → adicionar entrada
- Criar teste que valida que a opção existe no menu
- Verificar que o handler do menu chama createCiDataHub corretamente

### Fase R5 — Atualizar TECHDOC.md

**Problema:** Condição exige "Comente e documente o código". TECHDOC.md não tem seção CiDataHub.

**Ação:**
- Adicionar seção "CI DATA HUB" no TECHDOC.md com:
  - Visão geral da arquitetura
  - Interface CiDataHub (campos e tipos)
  - Função createCiDataHub (parâmetros, retorno)
  - Métricas derivadas e fórmulas
  - Padrão de fallback
  - Consumidores que usam CiDataHub
  - Diagrama de fluxo de dados

### Fase R6 — Correção de D5.5/D6.4

**Problema:** D5.5 (outlier treatment) parcial — saturação [0, 86400] aplicada apenas em avgDuration. D6.4 (progress feedback) nunca corrigido.

**Ação D5.5:**
- Verificar quais outras métricas precisam de tratamento de outliers
- Aplicar saturação ou percentil em métricas que não o têm
- Documentar critérios de outlier por métrica

**Ação D6.4:**
- Identificar o que D6.4 propõe (progress feedback)
- Implementar feedback de progresso durante criação do CiDataHub
- Testar que o feedback é exibido corretamente

### Fase R7 — Auditoria Pós-Implementação (Fase 18 original)

**Problema:** A auditoria completa do plano original não foi executada.

**Ação:** Executar TODAS as verificações da Fase 18:

#### 18.1 — Auditoria de integridade
- [ ] Todos os 21 consumidores aceitam `ciData?: CiDataHub`
- [ ] Todos os 21 consumidores têm fallback para MetricsStore
- [ ] Nenhum consumidor chama `loadMetrics()` diretamente quando CI disponível
- [ ] Nenhum consumidor calcula métricas que o hub já calculou

#### 18.2 — Auditoria de contrato
- [ ] `CiDataHub` interface é imutável (readonly)
- [ ] `createCiDataHub()` tipado corretamente
- [ ] Fallback funciona sem provider
- [ ] Persistência: StoreBackend.flush() chamado

#### 18.3 — Auditoria de cobertura
- [ ] `shared/ci-data.ts` ≥90% statements
- [ ] Todos os cenários Fase 13 presentes
- [ ] Todos os cenários Fase 14 presentes
- [ ] Todos os 21 consumidores têm cenários com CiDataHub

#### 18.4 — Auditoria de regressão
- [ ] PR report: mesmo output com dados do CI e com fallback
- [ ] Quality gate: mesmo resultado com dados do CI e com fallback
- [ ] Health score: mesmo score com dados do CI e com fallback
- [ ] Dashboards: mesmos dados com CiDataHub e com MetricsStore

### Fase R8 — Validação Final

**Ação:**
- `npx tsc --noEmit` → 0 erros
- `npx vitest run` → 100% pass
- `npm run lint` → 0 violações
- `npm run unused-exports` → 0 exports não utilizados
- `npx depcruise` → 0 violações
- Cobertura CiDataHub ≥90% statements
- Atualizar BACKLOG.md com status correto de CDH-15 e CDH-18
- Criar commit com todas as correções

## Dependências

```
R1 (consumer tests) ← sem dependências
R2 (system tests) ← depende de R1
R3 (e2e tests) ← depende de R2
R4 (menu verification) ← sem dependências
R5 (TECHDOC) ← sem dependências
R6 (D5.5/D6.4) ← sem dependências
R7 (audit) ← depende de R1-R6
R8 (validation) ← depende de R7
```

R1, R4, R5, R6 podem ser paralelos.

## Métricas Alvo

| Métrica | Alvo |
|---------|------|
| `npx tsc --noEmit` | 0 erros |
| `npx vitest run` | 100% pass |
| `npm run lint` | 0 violações |
| Consumer integration tests com ciData | ≥9 system tests |
| E2E tests | ≥3 cenários |
| Menu integration verified | sim |
| TECHDOC.md atualizado | sim |
| D5.5 outlier treatment completo | sim |
| D6.4 progress feedback implementado | sim |
| 18.1-18.4 auditoria completa | todos OK |
