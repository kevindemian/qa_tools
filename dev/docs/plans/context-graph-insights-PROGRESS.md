# PROGRESS — Plano de Reestruturação (Grafo de Contexto + Insights)

**Plano:** `dev/docs/plans/context-graph-insights-plan.md`
**Status:** Fase 0 em aberto (baseline F0.1 verde; fronteiras F0.2 decididas 2026-08-05; F0.3 completa; F0.4 completa; F0.5 completa; **F0.6 completa — próxima tarefa: F0.7**)

---

## Checkpoints

<!-- CHECKPOINT: Fase 0 complete -->

<!-- CHECKPOINT: Fase 1 complete -->

<!-- CHECKPOINT: Fase 2 complete -->

<!-- CHECKPOINT: Fase 3 complete -->

<!-- CHECKPOINT: Fase 4 complete -->

---

## Registro de execução

| Data | Fase.Tarefa | Ação | Resultado | Desvio |
|------|-------------|------|-----------|--------|
| 2026-08-05 | F0 (pré-baseline) | Confirmadas decisões Q1–Q3 + princípio "cálculos/campos sobrevivem por padrão; só renderers deletados" | IMPACT-PLAN §0 + plano principal §0.3 sincronizados; pendências P-1/P-2/P-3 registradas no IMPACT-PLAN | Nenhum |
| 2026-08-05 | F0.2 | Decididas fronteiras F-1/F-2/F-3 (P-2) + P-1 + P-3 — solução tecnicamente superior (instrução explícita do usuário) | Tabela F0.2 preenchida (plano principal §0.2); IMPACT-PLAN §0 tabela de pendências resolvidas; S2.2/S3.3/S6.4/S7.1 atualizados | Nenhum |
| 2026-08-05 | F0.1 | Baseline executado (HEAD `0272eb8a`): tsc ✅, vitest ✅ (545 arquivos / 7500 testes), lint ✅, depcruise ✅ (969 módulos / 3985 deps), unused-exports ✅, type-coverage ✅ (99.95% = 306625/306759), no-swallow ✅ | 7/8 verdes à primeira; `audit-suppressions` 🔴 preexistente | Nenhum |
| 2026-08-05 | F0.1 | Gap pré-existente tratado na raiz: 68 isenções expiradas em `audit/suppressions.yaml` (sunset 2026-07-23) — varredura com YAML vazio provou 0 swallows em arquivos lintados (só `scripts/validation-hook.ts`, fora do lint); arquivos movidos em refatorações (31) + catches corrigidos (37) → entradas stale removidas, `suppressions: []` | `audit-suppressions` ✅ exit 0 ("OK — nenhum contorno de segurança detectado"). `audit/` é gitignored → mudança local, sem commit | Nenhum |
| 2026-08-05 | F0.3 | TDD RED → migrados 3 cálculos p/ compute (`compute/ai-comparison.ts`, `compute/cross-squad-benchmark.ts`, `compute/requirement-score.ts`) + tipos + provenance; wrapper `compute/cross-squad.ts` deletado (fabrica vazio — Q3/Rule 25); barrel atualizado; `hub.ts` sem fabricações (`aiComparison`/`crossSquad` = `undefined`, Rule 25.2/P-3); consumidores repontados (interactive-mode, schedule-handler, renderers, scripts, quality-check allowlist, types/data-hub) | Teste de arquitetura `hub-architecture.test.ts` (novo, RED→GREEN). Gates: tsc ✅, vitest ✅ (544 arquivos / 7498 testes), lint ✅, depcruise ✅ (970 módulos), unused-exports ✅, type-coverage ✅ (99.95%), no-swallow ✅, audit-suppressions ✅. 2 testes do wrapper deletado removidos (`cross-squad.test.ts`, `cross-squad.integration.test.ts`); `hub.test.ts:364` passa a esperar `undefined` (P-3) | Hash de auto-integridade de `scripts/quality-check.ts` regenerado após alteração intencional da allowlist (mecanismo preservado, valor esperado atualizado) |
| 2026-08-05 | F0.4 | TDD RED (2 testes transformados: menu '25' não executa traceability em `dashboard-handlers.test.ts`; `getHandler('25')` nulo em `integration-menu-connectivity.test.ts`) → `case25.ts` deletado + cascata: `commands/index.ts` (import + registro '25'), `commands/case-d.ts` (import + entry de dashboard '25'), `menu-data.ts` (alias `traceability-matrix: '25'`), testes atualizados (bloco `Case25` e helpers `makeTraceabilityResult`/mock removidos; listas 'all 26 cases' e mock de traceability-limpos em integration) | Auditoria §0.4: `rg case25\|traceability jira_management/` (não-teste) = **0 refs**. Gates: tsc ✅, vitest ✅ (544 arquivos / 7489 testes), lint ✅, depcruise ✅ (969 módulos / 3982 deps), unused-exports ✅, type-coverage ✅ (99.95% = 306054/306188), no-swallow ✅, audit-suppressions ✅; eslint warnings 774 (= baseline, sem regressão); case17/case21 ✅ (37 testes) | **Premissa §0.4 corrigida:** case21/27 NÃO têm "export HTML" — só `default { handler }`, consumindo `generateCoverageGapHtml` (renderer PRESERVADO) inline → **nenhuma mudança** (Rule 7). **Isolamento de branch planejado (Q3) abortado:** `feat/f5-side-branch` diverge 237 arquivos de `main` (linha de integração longa: artifact-validation + mutation + F0.3 entrelaçados); cherry-pick de F0.3 sobre main em conflito (11+ arquivos) → F0.4 executado na branch de integração (SOP §22.1) |
| 2026-08-06 | F0.5 | TDD RED (4 testes novos que falham se entry de artefato deletado permanece: menu `interactive-mode` exclui `deletedLabels`; `ACTION_HANDLERS` sem `a`/`p`; `session-state.buildActionChoices` sem `a`/`p`; `schedule-handler` sem seções deletadas no HTML semanal). Implementação: `interactive-mode.ts` — imports de renderers deletados removidos, `handlePipelineHealthWrapper`/`_dashboardTraceabilityMatrix`/`_dashboardAiEffectiveness`/`_dashboardSilentRegression`/`_dashboardAiComparison`/`_dashboardBenchmark`/`_dashboardSuiteOptimization`/`_dashboardRequirementScore` deletados, menu reordenado 1-10 (8 sobreviventes + Quality Gate + Coverage Gap), `_testExports`/ACTION_HANDLERS atualizados; `schedule-handler.ts` — imports de deletados + `openWithFallback`/`calcFlakinessEntries`/`getDataHub` órfãos removidos, 7 seções HTML deletadas, `handleFlakinessDashboard` deletado; `session-state.ts` — entries `{a}`/`{p}` removidas do menu; `batch-mode.ts` — `generateFlakinessDashboard`/`generatePipelineHealthReport`/`handlePipelineHealth` deletados, imports órfãos removidos, dry-run reescrito (4 tópicos). Testes: describes de handlers deletados e mocks removidos em `interactive-mode.test.ts` (7 describes + HandlePipelineHealthWrapper), `main.test.ts` (HandleFlakinessDashboard + case `'a'` + expected keys), `schedule-handler.test.ts`, `batch-mode.test.ts`, `session-state.test.ts`, `integration-handlers.test.ts` | Auditoria §0.5: `rg "ai-effectiveness|ai-comparison|cross-squad|traceability|flakiness|suite-optimization|silent-regression|requirement-score|pipeline-health" git_triggers/ --glob '!**/__tests__/**'` = **6 refs, todos justificados** (ver Desvios). Gates: tsc ✅, vitest git_triggers ✅ (42 arquivos / 733 testes), lint ✅ (753 warnings = baseline, 0 novos), depcruise ✅ (971 módulos / 3973 deps), unused-exports ✅, no-swallow ✅, `.quality_ratchet.json` sem diff | **"9 seções" do plano ≠ realidade (7):** o relatório semanal renderizava apenas 7 seções dos deletados — `flakiness` e `pipeline-health` eram handlers de dashboard (batch/schedule), não seções semanais. **F-2 respeitado:** `generatePrReportIfNeeded` MANTIDO em `batch-mode.ts` (pr-report reconstruído em F1.3, CI+config preservados). **Warning de flakiness preservado** em `session-state.ts:261-264` (decisão do usuário — usa `computed.flakinessEntries`, compute sobrevivente por F0.3/F-3). **`pipeline-health-renderer.ts` NÃO deletado** (adiado p/ F0.6): consumido por `scripts/artifact-scorecard-runner.ts:43` + `scripts/artifact-validation-harness.ts:32`; remover agora quebraria build → renderer + 3 testes permanecem (F-3: renderer deletado, mas limpeza de scripts/harness é escopo F0.6). `git_triggers/main.ts` não referenciava os deletados (rg=0); `shared/ui/dashboard-menu.ts` genérico (DashboardDef). **`audit-suppressions` 🔴 preexistente** (fora do CI e da verificação F0.5): `audit/suppressions.yaml` gitignored foi removido localmente após F0.4 — arquivo ausente, gate FATAL; não relacionado a F0.5 |
| 2026-08-06 | F0.6 | TDD RED (teste novo do scorecard que falha se spec órfã permanece: "does not score or register any deleted artifact"; HTML_RENDERABLE_SPECS 19→10). Implementação: `scripts/artifact-scorecard-runner.ts` — imports/renderer entries dos 9 deletados removidos (pipeline-health, ai-effectiveness, ai-comparison, traceability, flakiness, suite-optimization, cross-squad-benchmark, silent-regression, requirement-score), `renderPipelineHealthOutput` deletado, scorecard passa a avaliar 10 (report-html + 8 sobreviventes + coverage-gap) + 5 unscored = 15; `scripts/artifact-validation-harness.ts` — imports/entries/blocos pipeline-health/flakiness-no-datahub dos deletados removidos, harness renderiza só artefatos vigentes; `scripts/quality-check.ts` `checkDashboardExports` — 14 entries dos deletados removidas, 14 mantidas (artefatos vigentes), HASH de auto-integridade regenerado (mecanismo preservado); `scripts/artifact-fixtures.ts` — docstring example `ai-effectiveness`→`backlog-health`. Testes: `artifact-scorecard-runner.test.ts` atualizado (10 scored, "15 total accounted", teste órfão novo), `quality-check.test.ts` integrity ok | Auditoria §0.6: `rg "ai-effectiveness|ai-comparison|cross-squad|traceability|flakiness|suite-optimization|silent-regression|requirement-score|pipeline-health" scripts/ | rg -v __tests__` = **8 refs em fixture DATA** (`"flakiness"` como campo de dado em `traceability.json`/`suite-optimization.json`) — não são referências de código; fixtures dos deletados são removidas em F0.7 (§0.7). Em código de scripts (`.ts` não-teste) = **0 refs**. Gates: tsc ✅, vitest scripts/__tests__ ✅ (13 arquivos / 210 testes), lint ✅, depcruise ✅ (971 módulos / 3947 deps), unused-exports ✅, no-swallow ✅, `.quality_ratchet.json` sem diff; scorecard funcional ✅ (10 scored, 0 failed, removable none), harness funcional ✅ (13 artefatos vigentes) | **Auditoria literal do plano não zera até F0.7:** o comando `rg ... scripts/ | rg -v __tests__` captura a palavra `flakiness` como dado dentro de fixtures de artefatos deletados (`scripts/__fixtures__/artefactos/traceability.json`, `suite-optimization.json`) — remoção dessas fixtures é escopo explícito de F0.7 (§0.7 CA: "fixtures sintéticas de deletados não existem"). F0.6 não antecipa F0.7 para preservar commit atômico |
