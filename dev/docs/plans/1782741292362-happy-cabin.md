# Plano: Corrigir erros ESLint restantes em código fonte

## Estado Atual
- 205 erros ESLint em código fonte (não-teste)
- TSC: 0 erros ✅
- Testes: 5757 pass ✅

## Arquivos e erros a corrigir

### shared/invariants/t-08.ts (5 erros)
- L17-19: `stepsText.match(regex)` → `regex.exec(stepsText)` (prefer-regexp-exec)
- L35: template literal aninhado → extrair variável

### shared/invariants/t-04.ts, t-05.ts, t-06.ts, t-10.ts (4 erros)
- Template literals aninhados → extrair variáveis

### shared/defect-seasonality.ts (2 erros)
- L182, L188: `!` non-null assertion → null check

### shared/metrics.ts (1 erro)
- L51: API Zod deprecada → usar alternativa moderna

### shared/shared-invariants.ts (2 erros)
- L7: TODO/FIXME tag → converter para issue ou implementar

### shared/llm-metrics.ts (1 erro)
- L256: selector parameter → métodos separados

### shared/http-client.ts (1 erro)
- L142: pseudo-random → Math.random() é aceitável aqui

### shared/llm-rate-limiter.ts (1 erro)
- L21: pseudo-random → Math.random() é aceitável aqui

### shared/open.ts (3 erros)
- L40, L69, L87: no-os-command-from-path

### shared/git-sha.ts (2 erros)
- L58, L77: no-os-command-from-path

### shared/test-impact.ts (2 erros)
- L48, L123: no-os-command-from-path

### jira_management/__mocks__/csv_resource.ts (1 erro)
- L5: public-static-readonly

### shared/__mocks__/output.ts (4 erros)
- L4-7: public-static-readonly

### jira_management/commands/case22.ts (1 erro)
- L11: no-os-command-from-path

### jira_management/commands/case07.ts, case12.ts, case15.ts, case17-helpers.ts, case19.ts
- no-nested-conditional

### git_triggers/ (múltiplos arquivos)
- no-nested-conditional, deprecation, no-unused-collection, no-labels, etc.

### cognitive-complexity (59 erros)
- Funções complexas demais → extrair helper functions

## Ordem de execução
1. Corrigir erros menores primeiro (prefer-regexp-exec, nested-template-literals, static-readonly, todo/fixme)
2. Corrigir no-nested-conditional (extrair ifs)
3. Corrigir no-os-command-from-path (validar PATH)
4. Corrigir cognitive-complexity (extrair helpers)
5. Rodar `npx tsc --noEmit && npx vitest run` para verificar

## Verificação
- `npx tsc --noEmit` → 0 erros
- `npx vitest run` → todos passam
- `npx eslint shared/ jira_management/ git_triggers/ --max-warnings=0` → 0 erros
