# Plano de Retomada — Plano 1782507364202-witty-falcon

## Situação Atual (2026-07-01)

### Ferramentas que PASSAM (6)
| Ferramenta | Estado |
|------------|--------|
| TypeScript (`tsc --noEmit`) | 0 erros |
| Vitest | 5757 pass, 9 skip |
| D7 Bad Testing | 14/14 PASS |
| Dependency Cruiser | 0 violações |
| Gitleaks | 0 leaks |
| Snyk | 1 high (brace-expansion) |

### Ferramentas com PROBLEMAS (4)

#### ESLint — 588 issues (161 errors, 427 warnings)
| Regra | Qtd | Severidade |
|-------|-----|-----------|
| `sonarjs/cognitive-complexity` | 60 | error |
| `sonarjs/no-os-command-from-path` | 46 | error |
| `security/detect-non-literal-fs-filename` | 419 | warning |
| `sonarjs/deprecation` | 18 | error |
| `sonarjs/publicly-writable-directories` | 10 | error |
| `sonarjs/super-linear-regex` | 9 | error |
| `security/detect-non-literal-regexp` | 6 | warning |
| `sonarjs/file-permissions` | 6 | error |
| `@typescript-eslint/no-non-null-assertion` | 2 | error |
| Outros sonarjs | 15 | error |

#### SOP Audit — 25 PASS, 42 FAIL
| Categoria | Qtd | Check |
|-----------|-----|-------|
| D9.4 (falsy fragility `\|\| 0`) | ~24 | 0.1.15 |
| D11.1 (new Date sem validação) | ~85 | 0.1.21 |
| D9.6 (JSON.parse/stringify) | 1 | 0.1.18 |
| D13.3 (var hoisting) | 1 | 0.1.24 |
| T14 (suppressions) | ~3 | 0.1.3 |
| D3.2 (DepWall imports diretos) | ~2 | 0.1.10 |
| T17 (env vars sem validação) | ~2 | 0.1.22/T17 |
| D8.4 (domain || 0) | ~2 | D8.4 |

#### Knip — 168 itens não usados
- 4 arquivos não usados
- 8 devDependencies não usados
- 54 exports não usados
- 110 exported types não usados
- 2 exported enum members não usados

#### Snyk — 1 issue
- `brace-expansion@5.0.6` → upgrade para 5.0.7

---

## Princípio de Correção

**"Não se quebra o espelho porque o rosto está sujo."**

As ferramentas DETECTAM erros. Os erros são no código. TODOS os erros devem ser corrigidos no código.
Zero supressão, zero desabilitação, zero bypass de regras de detecção.

---

## FASE 1 — Quick Wins (automatizável, ~30min)

### 1.1 Snyk: upgrade brace-expansion
```bash
npm update brace-expansion
```

### 1.2 Fix var → const (1 ocorrência)
- `shared/html-factory.ts:58` — `var _toggleTheme` → `const _toggleTheme`

### 1.3 Fix non-null assertions (2 ocorrências)
- Usar optional chaining ou null check
- Arquivos: identificar via `npx eslint . --rule '@typescript-eslint/no-non-null-assertion: error'`

### 1.4 Prettier: formatação automática
```bash
npx prettier --write '**/*.{ts,js,json}'
```

**Verificação:** `npx vitest run` (deve manter 5757+ pass)

---

## FASE 2 — ESLint: sonarjs/cognitive-complexity (60 errors, ~4h)

Refatorar funções com complexidade > 15. Extrair funções menores.

**Arquivos prioritários (estimativa baseada nos 60 errors):**
- `git_triggers/batch-mode.ts` — complexidade 25
- `git_triggers/cli-args.ts` — complexidade 27
- `git_triggers/schedule-handler.ts` — complexidade 23
- `jira_management/commands/case15.ts` — complexidade 16
- `jira_management/commands/case17-helpers.ts` — complexidade 24
- `jira_management/commands/case17-test-utils.ts` — complexidade 16-17
- `e2e/gen-report-complete.ts` — complexidade 19
- Outros ~50 arquivos

**Estratégia:** Para cada arquivo:
1. Identificar funções com complexidade > 15
2. Extrair funções auxiliares menores (cada uma com complexidade ≤ 15)
3. Manter comportamento idêntico
4. Rodar testes para validar

**Verificação:** `npx eslint . --rule 'sonarjs/cognitive-complexity: error'` → 0 errors

---

## FASE 3 — ESLint: sonarjs/no-os-command-from-path (46 errors, ~2h)

Corrigir uso de `process.env.PATH` sem validação.

**Estratégia:** Em cada arquivo:
1. Identificar chamadas a `execFile`/`execFileSync` com paths do PATH
2. Resolver caminhos absolutos via `which` ou usar paths fixos
3. Validar existência antes de usar

**Verificação:** `npx eslint . --rule 'sonarjs/no-os-command-from-path: error'` → 0 errors

---

## FASE 4 — ESLint: security/detect-non-literal-fs-filename (419 warnings, ~4h)

Tipar argumentos de `fs.*Sync()` — usar `string` type annotation ou validar com `path.resolve()`.

**Estratégia:** Para cada ocorrência:
1. Verificar se o argumento já tem tipo `string` explícito
2. Se não tem, adicionar tipo ou usar `path.resolve()` para garantir string
3. Priorizar validação com path.resolve para defesa em profundidade

**Verificação:** `npx eslint . --rule 'security/detect-non-literal-fs-filename: warn'` → 0 warnings

---

## FASE 5 — ESLint: sonarjs/deprecation (18 errors, ~1h)

Substituir APIs deprecadas por equivalentes modernas.

**Verificação:** `npx eslint . --rule 'sonarjs/deprecation: error'` → 0 errors

---

## FASE 6 — ESLint: sonarjs/publicly-writable-directories (10 errors, ~30min)

Validar paths de diretórios temporários antes de usar.

**Verificação:** `npx eslint . --rule 'sonarjs/publicly-writable-directories: error'` → 0 errors

---

## FASE 7 — ESLint: sonarjs/super-linear-regex (9 errors, ~1h)

Otimizar regex com backtracking potencial.

**Verificação:** `npx eslint . --rule 'sonarjs/super-linear-regex: error'` → 0 errors

---

## FASE 8 — SOP Audit Corrections (~4h)

### 8.1 `|| 0` → `?? 0` (24 ocorrências)
Verificar semântica: se 0 é valor válido no domínio, usar `?? 0`.

### 8.2 `new Date(string)` — adicionar validação (~85 ocorrências)
Adicionar `isNaN(d.getTime())` onde argumento é string.

### 8.3 `JSON.parse(JSON.stringify(x))` → `structuredClone(x)` (1 ocorrência)
- `shared/state.ts:171`

### 8.4 `as unknown` em source (3 ocorrências)
- `shared/splash.ts:37` — usar type guard
- `git_triggers/gitlab-workflow.ts:114` — tipar corretamente
- `shared/report-validator.ts:112,124` — usar type guard

### 8.5 DepWall: imports diretos de `fs` (2 ocorrências)
- `git_triggers/ai-test-impact.ts:2` — usar `shared/deps`
- `git_triggers/pr-report-setup-handler.ts:1` — usar `shared/deps`

### 8.6 Env vars sem validação (2 ocorrências)
- `git_triggers/batch-mode.ts:168` — adicionar schema validation
- `git_triggers/pipeline-jira.ts:17` — adicionar schema validation

**Verificação:** `bash scripts/audit/sop-audit.sh --all` → 0 FAIL

---

## FASE 9 — Knip Cleanup (~2h)

### 9.1 Remover 4 arquivos não usados
### 9.2 Remover 8 devDependencies não usados
### 9.3 Remover 54 exports não usados
### 9.4 Remover 110 exported types não usados
### 9.5 Remover 2 exported enum members não usados

**Verificação:** `npx knip` → 0 unused

---

## FASE 10 — Outros ESLint Errors (~2h)

- `sonarjs/file-permissions` (6 errors) — corrigir permissões
- `sonarjs/no-invariant-returns` (2 errors)
- `sonarjs/no-nested-functions` (2 errors)
- `sonarjs/no-element-overwrite` (2 errors)
- `sonarjs/pseudo-random` (2 errors)
- `sonarjs/no-nested-conditional` (1 error)
- `sonarjs/function-return-type` (1 error)
- `security/detect-object-injection` (2 warnings)
- `security/detect-non-literal-regexp` (6 warnings)

---

## Verificação Final

```bash
npx tsc --noEmit                        # 0 erros
npx eslint .                            # 0 errors, 0 warnings
npx vitest run                          # 5757+ pass, 0 fail
bash scripts/audit/sop-audit.sh --all   # 0 FAIL
bash scripts/audit/d7-bad-testing.sh --all  # 14/14 PASS
npx knip                                # 0 unused
npx dependency-cruise . --config .dependency-cruiser.js  # 0 violations
gitleaks detect --no-banner             # 0 leaks
npx snyk test --severity-threshold=high # 0 issues
```

## Ordem de Execução

```
1. Fase 1 (Quick Wins) — 30min
2. Fase 2 (cognitive-complexity) — 4h
3. Fase 3 (no-os-command-from-path) — 2h
4. Fase 4 (detect-non-literal-fs-filename) — 4h
5. Fase 5 (deprecation) — 1h
6. Fase 6 (publicly-writable-directories) — 30min
7. Fase 7 (super-linear-regex) — 1h
8. Fase 8 (SOP corrections) — 4h
9. Fase 9 (knip cleanup) — 2h
10. Fase 10 (outros ESLint) — 2h
11. Verificação final
```

**Total estimado: ~21h de trabalho**

## Arquivos Críticos a Modificar

**ESLint errors (~80 arquivos):**
- `git_triggers/batch-mode.ts` — complexidade + vários
- `git_triggers/cli-args.ts` — complexidade
- `git_triggers/schedule-handler.ts` — complexidade
- `git_triggers/ai-test-impact.ts` — DepWall + fs
- `git_triggers/pr-report-setup-handler.ts` — DepWall + fs
- `git_triggers/pipeline-health.ts` — regex
- `git_triggers/gitlab-workflow.ts` — as unknown
- `jira_management/commands/case15.ts` — complexidade
- `jira_management/commands/case17-helpers.ts` — complexidade
- `jira_management/commands/case17-test-utils.ts` — complexidade
- `e2e/gen-report-complete.ts` — complexidade + nested ternary
- `shared/html-factory.ts` — var
- `shared/state.ts` — JSON.parse/stringify
- `shared/splash.ts` — as unknown
- `shared/report-validator.ts` — as unknown
- ~24 arquivos com `|| 0`
- ~85 arquivos com `new Date(string)`

**Knip cleanup:**
- `knip.json` — atualizar config
- `package.json` — remover devDependencies
- ~54 arquivos com exports não usados
- ~110 arquivos com types não usados
