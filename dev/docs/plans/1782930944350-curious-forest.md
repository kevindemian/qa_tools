# Plano de Retomada — Correções Sistemáticas

## Situação Atual (2026-07-01)

### Estado das Ferramentas

| Ferramenta | Estado Atual | Meta |
|------------|--------------|------|
| TypeScript | 0 erros | 0 erros ✅ |
| Vitest | 5707 pass, 9 skip, 1 OOM | 5757+ pass, 0 fail |
| SOP Audit | 29 PASS, 38 FAIL | 0 FAIL |
| Knip | ~50+ itens não usados | 0 unused |
| ESLint | Timeout na execução | 0 errors, 0 warnings |

### OOM Error no Vitest
O erro OOM (Out of Memory) é causado pelo worker fork do Vitest. Não é bug de código — é limitação de memória do ambiente. Considerar aumentar `NODE_OPTIONS=--max-old-space-size=4096` ou executar testes em batches menores.

---

## FASE 2 — ESLint: sonarjs/cognitive-complexity (60 errors)

**Objetivo:** Reduzir complexidade cognitiva de funções > 15 para ≤ 15.

**Arquivos prioritários:**
- `git_triggers/batch-mode.ts` — complexidade 25
- `git_triggers/cli-args.ts` — complexidade 27
- `git_triggers/schedule-handler.ts` — complexidade 23
- `jira_management/commands/case15.ts` — complexidade 16
- `jira_management/commands/case17-helpers.ts` — complexidade 24
- `jira_management/commands/case17-test-utils.ts` — complexidade 16-17
- `e2e/gen-report-complete.ts` — complexidade 19

**Estratégia:**
1. Para cada arquivo com complexidade > 15:
   - Extrair funções auxiliares menores
   - Cada nova função com complexidade ≤ 15
   - Manter comportamento idêntico
2. Rodar testes após cada refatoração

**Verificação:**
```bash
npx eslint . --rule 'sonarjs/cognitive-complexity: error' 2>&1 | grep "cognitive-complexity" | wc -l
# Resultado esperado: 0
```

---

## FASE 3 — ESLint: sonarjs/no-os-command-from-path (46 errors)

**Objetivo:** Corrigir uso de `process.env.PATH` sem validação.

**Estratégia:**
1. Identificar chamadas a `execFile`/`execFileSync` com paths do PATH
2. Resolver caminhos absolutos via `which` ou usar paths fixos
3. Validar existência antes de usar

**Arquivos-alvo:**
- `git_triggers/batch-mode.ts`
- `git_triggers/schedule-handler.ts`
- `git_triggers/cli-args.ts`

**Verificação:**
```bash
npx eslint . --rule 'sonarjs/no-os-command-from-path: error' 2>&1 | grep "no-os-command" | wc -l
# Resultado esperado: 0
```

---

## FASE 7 — ESLint: sonarjs/super-linear-regex (9 errors)

**Objetivo:** Otimizar regex com backtracking potencial.

**Arquivos:**
- `git_triggers/pipeline-health.ts`
- Outros arquivos com regex problemáticas

**Estratégia:**
1. Identificar regex com backtracking
2. Reescrever para evitar catastrofe
3. Usar atomic groups ou possessive quantifiers quando possível

**Verificação:**
```bash
npx eslint . --rule 'sonarjs/super-linear-regex: error' 2>&1 | grep "super-linear" | wc -l
# Resultado esperado: 0
```

---

## FASE 8 — SOP Audit Corrections (38 FAIL restantes)

### 8.1 `|| 0` → `?? 0` (~24 ocorrências restantes)
Verificar semântica: se 0 é valor válido no domínio, usar `?? 0`.

### 8.2 `new Date(string)` — adicionar validação (~85 ocorrências restantes)
Adicionar `isNaN(d.getTime())` onde argumento é string.

### 8.3 Env vars sem validação (2 ocorrências)
- `git_triggers/batch-mode.ts:168` — adicionar schema validation
- `git_triggers/pipeline-jira.ts:17` — adicionar schema validation

**Verificação:**
```bash
bash scripts/audit/sop-audit.sh --all 2>&1 | grep "FAIL" | wc -l
# Resultado esperado: 0
```

---

## FASE 9 — Knip Cleanup (~50+ itens restantes)

### 9.1 Remover arquivos não usados
### 9.2 Remover exports não usados
### 9.3 Remover exported types não usados

**Verificação:**
```bash
npx knip 2>&1 | grep -E "^(Unused|Unused exports)" | wc -l
# Resultado esperado: 0
```

---

## FASE 10 — Outros ESLint Errors

- `sonarjs/file-permissions` (6 errors)
- `sonarjs/no-invariant-returns` (2 errors)
- `sonarjs/no-nested-functions` (2 errors)
- `sonarjs/no-element-overwrite` (2 errors)
- `sonarjs/pseudo-random` (2 errors)
- `sonarjs/no-nested-conditional` (1 error)
- `sonarjs/function-return-type` (1 error)
- `security/detect-object-injection` (2 warnings)
- `security/detect-non-literal-regexp` (6 warnings)

---

## Ordem de Execução Recomendada

```
1. Commit das mudanças atuais (6 arquivos)
2. FASE 2 (cognitive-complexity) — maior impacto
3. FASE 3 (no-os-command-from-path)
4. FASE 7 (super-linear-regex)
5. FASE 8 (SOP corrections)
6. FASE 9 (knip cleanup)
7. FASE 10 (outros ESLint)
8. Verificação final
```

---

## Verificação Final

```bash
npx tsc --noEmit                        # 0 erros
npx eslint .                            # 0 errors, 0 warnings
npx vitest run                          # 5757+ pass, 0 fail
bash scripts/audit/sop-audit.sh --all   # 0 FAIL
npx knip                                # 0 unused
```

---

## Arquivos Críticos a Modificar

**ESLint cognitive-complexity (~15 arquivos):**
- `git_triggers/batch-mode.ts`
- `git_triggers/cli-args.ts`
- `git_triggers/schedule-handler.ts`
- `jira_management/commands/case15.ts`
- `jira_management/commands/case17-helpers.ts`
- `jira_management/commands/case17-test-utils.ts`
- `e2e/gen-report-complete.ts`

**no-os-command-from-path (~10 arquivos):**
- `git_triggers/batch-mode.ts`
- `git_triggers/schedule-handler.ts`
- `git_triggers/cli-args.ts`

**super-linear-regex (~5 arquivos):**
- `git_triggers/pipeline-health.ts`

**SOP corrections (~24 arquivos com || 0):**
- Diversos arquivos em shared/, git_triggers/, jira_management/

---

## Notas Importantes

1. **OOM Error**: O Vitest está atingindo limite de memória. Considere:
   - Aumentar `NODE_OPTIONS=--max-old-space-size=4096`
   - Ou executar testes em batches: `npx vitest run --reporter=verbose src/**/*.test.ts`

2. **Mudanças Pendentes**: Existem 6 arquivos modificados não commitados. Recomenda-se commitar antes de prosseguir.

3. **Complexidade**: A FASE 2 é a mais demorada (~4h). Priorize arquivos com maior complexidade primeiro.
