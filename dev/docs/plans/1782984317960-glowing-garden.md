# Baseline Completa — 2026-07-02 (Re-executada)

## Resumo Executivo — Comparativo

| Categoria            | Antes           | Agora           | Delta             | Notas                                     |
| -------------------- | --------------- | --------------- | ----------------- | ----------------------------------------- |
| **TypeScript**       | ✅ 0 erros      | ❌ 2 erros      | 📈 REGRESSION     | `safe-fs.ts` — tipos errados em `node:fs` |
| **Vitest**           | ⚠️ 5706 pass    | ⚠️ 5707 pass    | ✅ Mesmo patamar  | OOM em `prompt.test.ts`, 9 skip           |
| **Quality Gate**     | ✅ 13/13        | ❌ 2 violations | 📈 REGRESSION     | eslint falha por `safe-fs.ts`             |
| **Semgrep**          | ❌ 159 findings | ❌ 116 findings | 📉 -43            | 2 ERROR, 114 WARNING                      |
| **Snyk**             | ✅ 0 vuln       | ⚠️ 1 vuln       | 📈 NOVO           | uuid@13.0.2 via @mimo-ai/plugin           |
| **OSV Scanner**      | ✅ 0 issues     | ✅ 0 issues     | ➡️ Estável        | 474 packages                              |
| **GitLeaks**         | ✅ 0 leaks      | ✅ 0 leaks      | ➡️ Estável        | 1307 commits                              |
| **npm audit**        | ✅ 0 vuln       | ✅ 0 vuln       | ➡️ Estável        | Limpo                                     |
| **Knip**             | ❌ ~50+ unused  | ❌ 151 unused   | 📈 +100           | 41 exports + 110 types                    |
| **Dep Cruiser**      | ✅ 0 violations | ✅ 0 violations | ➡️ Estável        | 688 modules                               |
| **Type Coverage**    | ✅ 99.95%       | ✅ 99.95%       | ➡️ Estável        | 221782/221891                             |
| **Prettier**         | —               | ⚠️ 12 files     | 🆕 NOVO           | Files not formatted                       |
| **Structural Audit** | ✅ 0 dup        | ✅ 0 dup        | ➡️ Estável        | warnings rg ENOENT                        |
| **UX Auditor**       | ⚠️ 17 dead      | ⚠️ 17 dead      | ➡️ Estável        | Friction score 1                          |
| **Registry**         | ✅ Valid        | ✅ Valid        | ➡️ Estável        | model-registry.json                       |
| **D7 Bad Testing**   | ✅ 14/14        | ✅ 14/14        | ➡️ Estável        | Perfeito                                  |
| **SonarCloud**       | ❌ offline      | ❌ auth fail    | ➡️ Token inválido | `Not authorized`                          |
| **Security Logs**    | ⚠️ 235 bypass   | ⚠️ 235 bypass   | ➡️ Estável        | + bug P0 no script                        |

---

## Análise Detalhada por Ferramenta

### 1. TypeScript — ❌ 2 erros (REGRESSION)

```
shared/safe-fs.ts(30,61): error TS2724: '"node:fs"' has no exported member named 'MkdirSyncOptions'. Did you mean 'RmDirOptions'?
shared/safe-fs.ts(38,58): error TS2724: '"node:fs"' has no exported member named 'RmSyncOptions'. Did you mean 'RmOptions'?
```

- Causa: `safe-fs.ts` importa `MkdirSyncOptions` e `RmSyncOptions` que não existem em `node:fs`
- Correção: Usar `MakeDirectoryOptions` e `RmOptions` (ou `RmDirOptions`)

### 2. Vitest — ⚠️ OOM (1 error)

- **5707 passed**, 9 skipped, **1 error** (não é fail de teste)
- Erro: `JavaScript heap out of memory` em `shared/prompt.test.ts`
- Worker fork crash por OOM — Vitest pool timeout
- Não é bug de código — limitação de memória

### 3. Quality Gate — ❌ 2 violations

```
❌ eslint (zero violations) — 2 violation(s)
   shared/safe-fs.ts:31 — Unsafe argument (MakeDirectoryOptions)
   shared/safe-fs.ts:39 — Unsafe argument (RmOptions)
✅ 3-way handler consistency
✅ throw string
✅ vi.fn type safety
✅ artifact validation
✅ non-null assertion
✅ DepWall
✅ if(true/false)
✅ auto-integrity
```

- Causa raiz: mesmo do TypeScript — `safe-fs.ts` com tipos errados

### 4. Semgrep — ❌ 116 findings (116 blocking)

| Severidade | Count | Regra                         |
| ---------- | ----- | ----------------------------- |
| WARNING    | 109   | `path-join-resolve-traversal` |
| WARNING    | 5     | `detect-non-literal-regexp`   |
| ERROR      | 1     | `gcm-no-tag-length`           |
| ERROR      | 1     | `detect-child-process`        |

**Arquivos mais afetados:**

- `shared/state.ts` (16 findings)
- `jira_management/mapping-file-generator.ts` (9)
- `shared/disk-cache.ts` (9)
- `shared/temp-dir.ts` (9)
- `shared/ai-feedback.ts` (8)

**Findings críticos (ERROR):**

1. `shared/disk-cache.ts:64` — GCM decryption sem auth tag length
2. `shared/open.ts:137` — Command injection via child_process

### 5. Snyk — ⚠️ 1 vulnerabilidade

```
✗ Improper Validation of Specified Index, Position, or Offset in Input
  [Medium Severity][SNYK-JS-UUID-16133035]
  in uuid@13.0.2
  introduced by:
    - @mimo-ai/plugin@0.1.1 > effect@4.0.0-beta.48 > uuid@13.0.2
    - @opencode-ai/plugin@1.15.7 > effect@4.0.0-beta.66 > uuid@13.0.2
  Fixed in: 11.1.1, 14.0.0
```

- Projetos afetados: root + .mimocode + .opencode
- Dependência indireta (via effect) — não há upgrade direto disponível

### 6. OSV Scanner — ✅ 0 issues

- 474 packages scanned, sem vulnerabilidades

### 7. GitLeaks — ✅ 0 leaks

- 1307 commits, 18.59 MB

### 8. npm audit — ✅ 0 vulnerabilities

- 477 dependencies (85 prod, 392 dev, 104 optional)

### 9. Knip — ❌ 151 unused (41 exports + 110 types)

**Unused files:** `shared/safe-fs.ts`
**Unused exports (41):** incluindo `getUserChoice`, `generatePostProcessWorkflowFromContext`, `MeaningfulChangeSchema`, `PRIORITY_WEIGHTS`, re-exports de deps, etc.
**Unused types (110):** `CliMode`, `BaseCliArgs`, `RunStats`, `VersionData`, `JiraIssue`, `MeaningfulChange`, `ConfigFieldString`, etc.
**Config hints (5):** snyk/gitleaks/osv-scanner em ignoreDependencies + dist/ entry files not found

### 10. Dependency Cruiser — ✅ 0 violations

- 688 modules, 2690 dependencies

### 11. Type Coverage — ✅ 99.95%

- 221782 / 221891 tipos cobertos

### 12. Prettier — ⚠️ 12 files not formatted

```
.prettierrc, BACKLOG-historico.md, BACKLOG.md,
git_triggers/session-state.ts, jira_management/import-prep-parsers.ts,
jira_management/precondition-importer.ts, jira_xray_config_backup.md,
knip.json, shared/.local/state/qa-tools/state.json,
shared/invariants/t-02.ts, shared/safe-fs.ts, shared/show-docs.ts
```

### 13. Structural Audit — ✅ 0 duplications

- Todos patterns count 0
- warnings: `spawnSync /usr/bin/rg ENOENT` (ripgrep não encontrado)

### 14. UX Auditor — ⚠️ Friction score 1

- 17 dead utilities (test-only exports)
- Mesmos arquivos do baseline anterior

### 15. Registry Verifier — ✅ Valid

### 16. D7 Bad Testing — ✅ 14/14 PASS

- Todos os checks passando

### 17. SonarCloud — ❌ Not authorized

```
ERROR: Not authorized. Please check the properties sonar.login and sonar.password.
```

- Token `SONAR_TOKEN` do `.env.local` é inválido ou expirado
- Scanner tentou conectar em sonarcloud.io mas falhou na autenticação

### 18. Security Log Scanner — ⚠️ Bug P0 + Histórico

- **Bug P0:** Syntax error na linha 71 (`scripts/scan-sec-logs.sh`)
- Expressão aritmetica mal formada: `$(())` recebe output multi-line do `wc -l`
- 235 commits com bypass flags no histórico
- 1077 entries no security violations log
- Warden audit log não encontrado

---

## Arquivos Modificados (Unstaged)

| Arquivo                                   | Mudança                            | Status                                  |
| ----------------------------------------- | ---------------------------------- | --------------------------------------- |
| `shared/safe-fs.ts`                       | Types errados de node:fs           | **BLOQUEANTE — causa raiz de 3 falhas** |
| `package.json`                            | Adicionados 6 scripts de segurança | Pendente commit                         |
| `git_triggers/github_manager.ts`          | Non-null assertions → safe init    | Pendente commit                         |
| `git_triggers/nivelar.ts`                 | Promise.all → Promise.allSettled   | Pendente commit                         |
| `scripts/quality-check.ts`                | Regex fix + hash update            | Pendente commit                         |
| `sonar-project.properties`                | Adicionado sonar.organization      | Pendente commit                         |
| `shared/.local/state/qa-tools/state.json` | Timestamps atualizados             | Pendente commit                         |

---

## Plano de Ação (Priorizado — Atualizado)

### Fase 1: Corregir Regression em safe-fs.ts (IMEDIATO — BLOQUEANTE)

**Causa raiz de 3 falhas:** TypeScript (2 errors) + Quality Gate (2 violations)

1. Corrigir `shared/safe-fs.ts:30` — `MkdirSyncOptions` → `MakeDirectoryOptions`
2. Corrigir `shared/safe-fs.ts:38` — `RmSyncOptions` → `RmOptions`
3. Rodar `npx tsc --noEmit` para confirmar 0 erros
4. Rodar `npm run lint` para confirmar 0 violations

### Fase 2: Semgrep Findings Críticos (ALTA PRIORIDADE)

2 findings ERROR:

1. `shared/disk-cache.ts:64` — GCM missing auth tag length
2. `shared/open.ts:137` — Command injection via child_process

109 findings WARNING (path-traversal):

- Priorizar: `shared/state.ts` (16), `jira_management/mapping-file-generator.ts` (9)
- Avaliar se inputs já são sanitizados upstream

### Fase 3: Snyk — uuid@13.0.2 (MÉDIA PRIORIDADE)

- Dependência indireta via effect (em @mimo-ai/plugin e @opencode-ai/plugin)
- Upgrade disponível: uuid@14.0.0
- Verificar se effect@latest já usa uuid@14
- Se não, reportar upstream

### Fase 4: SonarCloud Auth (BAIXA PRIORIDADE)

- Token `SONAR_TOKEN` inválido/expirado
- Gerar novo token em sonarcloud.io
- Atualizar `.env.local`

### Fase 5: Security Log Scanner Bug P0 (BAIXA PRIORIDADE)

- Bug na linha 71 de `scripts/scan-sec-logs.sh`
- Corrigir expressão aritmetica: capturar output do `wc -l` corretamente

### Fase 6: Knip Cleanup (MÉDIA PRIORIDADE)

- 151 unused (41 exports + 110 types)
- Priorizar: tipos de theme tokens, validators, xray types
- Remover `shared/safe-fs.ts` se unused

### Fase 7: Prettier (BAIXA PRIORIDADE)

- 12 arquivos não formatados
- Rodar `npx prettier --write .`

### Fase 8: Vitest OOM (BAIXA PRIORIDADE)

- Worker crash em `shared/prompt.test.ts`
- Considerar `NODE_OPTIONS=--max-old-space-size=4096`

---

## Verificação Pós-Implementation

```bash
# Core (Fase 1)
npx tsc --noEmit                    # 0 erros (era 2)
npm run lint                        # 0 violations (era 2)

# Segurança (Fase 2)
semgrep scan --config auto .        # 0 findings ERROR (era 2)
snyk test --all-projects            # 0 vulnerabilities (era 1)

# Qualidade
npx vitest run                      # 5707+ pass, 0 errors
npm run depcruise                   # 0 violations
npx type-coverage --at-least 95     # >=95%
npx prettier --check .              # 0 warnings

# Auditoria
bash scripts/audit/d7-bad-testing.sh --all # 14 PASS
npx tsx scripts/audit/structural.ts        # 0 duplications
npx tsx scripts/ux-auditor.ts              # friction acceptable
npx tsx scripts/verify-registry.ts         # valid
```
