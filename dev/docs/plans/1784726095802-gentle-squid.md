# Separação Backend → HTML: Plano de Implementação Detalhado

## Contexto

O projeto gera relatórios HTML estáticos. A separação atual entre dados e HTML é **parcial** — 16 módulos misturam computação com geração de HTML no mesmo arquivo. A refatoração visa criar uma camada HTML limpa que consome dados do backend, cobrindo **TODOS** os artefatos, relatórios, dashboards e produtos gerados.

**Padrão de separação:** Cada módulo que gera HTML extrai a função render para um novo arquivo `*-renderer.ts`. O arquivo original vira barrel que re-exporta compute + render.

---

## FASE 0: Contrato de Dados Centralizado

### Tarefa 0.1: Criar interfaces compartilhadas

**Arquivo a criar:** `shared/report/types/report-data.ts`

**Conteúdo:**
```typescript
/**
 * Shared data contracts between compute and render layers.
 * @module report-data
 */

/** A single HTML section within a dashboard. */
export interface SectionResult {
  title: string;
  content: string; // HTML fragment
}

/** Common shape for all dashboard renderers. */
export interface DashboardData {
  summaryCards: Array<{ label: string; value: string; severity?: string }>;
  sections: SectionResult[];
  timestamp: string;
}
```

**Verificação:**
- `npx tsc --noEmit` — 0 erros

**Commit:** `feat(report): add shared data contracts for compute/render separation`

---

### Tarefa 0.2: Atualizar barrel de tipos

**Arquivo a modificar:** `shared/report/report-types.ts`

**Mudança:** Adicionar re-export das novas interfaces:
```typescript
export type { SectionResult, DashboardData } from './types/report-data.js';
```

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run` — 100% pass

**Commit:** `feat(report): re-export shared data contracts from report-types`

---

## FASE 1: Separar Report Modules

### Tarefa 1.1: Extrair ai-effectiveness-renderer

**Arquivo a criar:** `shared/report/ai-effectiveness-renderer.ts`

**Mover de `ai-effectiveness.ts` para cá:**
- Função `generateAiEffectivenessHtml()` (linha 109)
- Função `buildMetricLarge()` (linha 142)
- Função `buildVersionTable()` (linha 150)
- Função `buildTrendTable()` (linha 173)

**Imports do novo arquivo:**
```typescript
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from './html-factory.js';
import { buildCss } from './report-styles.js';
import { rootLogger } from '../logger.js';
import type { AiEffectivenessResult } from './ai-effectiveness.js';
```

**Exports do novo arquivo:**
```typescript
export function generateAiEffectivenessHtml(
  result: AiEffectivenessResult | null | undefined,
  title?: string
): string;
```

**Arquivo a modificar:** `shared/report/ai-effectiveness.ts`

**Remover:**
- Função `generateAiEffectivenessHtml()` (linha 109-140)
- Função `buildMetricLarge()` (linha 142-148)
- Função `buildVersionTable()` (linha 150-171)
- Função `buildTrendTable()` (linha 173-194)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`

**Adicionar re-export:**
```typescript
export { generateAiEffectivenessHtml } from './ai-effectiveness-renderer.js';
```

**Manter no arquivo original:**
- Interface `AiFeedbackRecord` (privada)
- Interface `AiFeedbackStore` (privada)
- Interface `AiEffectivenessResult` (exportada)
- Função `computeAiEffectiveness()` (exportada)
- Função `convertGenerationRecordsToFeedback()` (exportada)
- Type `AiGenerationRecord` import (para `convertGenerationRecordsToFeedback`)

**Arquivos a modificar (imports):**
- `git_triggers/schedule-handler.ts` — manter import de `computeAiEffectiveness` + `generateAiEffectivenessHtml` (barrel)
- `git_triggers/interactive-mode.ts` — manter import de `generateAiEffectivenessHtml` + `computeAiEffectiveness` (barrel)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/ai-effectiveness.test.ts` — pass
- `npx vitest run shared/__tests__/ai-effectiveness.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/ai-effectiveness.integration.test.ts` — pass

**Commit:** `feat(report): extract ai-effectiveness-renderer from ai-effectiveness`

---

### Tarefa 1.2: Extrair ai-comparison-renderer

**Arquivo a criar:** `shared/report/ai-comparison-renderer.ts`

**Mover de `ai-comparison.ts`:**
- Função `generateAiComparisonHtml()` (linha 194)
- Função `buildComparisonCards()` (linha 140)
- Função `buildAdvantageSection()` (linha 152)
- Função `buildVersionTable()` (linha 173)

**Imports do novo arquivo:**
```typescript
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from './html-factory.js';
import { buildCss } from './report-styles.js';
import { rootLogger } from '../logger.js';
import { MetricCard, MetricGrid, Badge, DataTable } from '../primitives/index.js';
import type { TableColumn, TableRow } from '../primitives/index.js';
import type { AiComparisonResult } from './ai-comparison.js';
```

**Exports do novo arquivo:**
```typescript
export function generateAiComparisonHtml(
  result: AiComparisonResult | null | undefined,
  title?: string
): string;
```

**Arquivo a modificar:** `shared/report/ai-comparison.ts`

**Remover:**
- Função `generateAiComparisonHtml()` (linha 194-231)
- Função `buildComparisonCards()` (linha 140-150)
- Função `buildAdvantageSection()` (linha 152-171)
- Função `buildVersionTable()` (linha 173-192)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`, `Badge`, `DataTable`, `TableColumn`, `TableRow`

**Adicionar re-export:**
```typescript
export { generateAiComparisonHtml } from './ai-comparison-renderer.js';
```

**Manter no arquivo original:**
- Interface `AiComparisonRecord` (exportada)
- Interface `AiComparisonResult` (exportada)
- Interface `GroupSummary` (privada)
- Função `summarizeGroup()` (privada)
- Função `compareAiVsManual()` (exportada)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/ai-comparison.test.ts` — pass
- `npx vitest run shared/__tests__/ai-comparison.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/ai-comparison.integration.test.ts` — pass

**Commit:** `feat(report): extract ai-comparison-renderer from ai-comparison`

---

### Tarefa 1.3: Extrair incident-report-renderer

**Arquivo a criar:** `shared/report/incident-report-renderer.ts`

**Mover de `incident-report.ts`:**
- Função `generateIncidentReportHtml()` (linha 191)
- Função `severityToCardSeverity()` (linha 62)

**Imports do novo arquivo:**
```typescript
import { rootLogger } from '../logger.js';
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from './html-factory.js';
import { buildCss } from './report-styles.js';
import { MetricCard, MetricGrid, Card } from '../primitives/index.js';
import { extractErrorMessage } from '../ui/prompt-errors.js';
import type { IncidentReport } from './incident-report.js';
```

**Exports do novo arquivo:**
```typescript
export function generateIncidentReportHtml(
  report: IncidentReport | null | undefined,
  title?: string
): string;
```

**Arquivo a modificar:** `shared/report/incident-report.ts`

**Remover:**
- Função `generateIncidentReportHtml()` (linha 191-278)
- Função `severityToCardSeverity()` (linha 62-67)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`, `Card`, `extractErrorMessage`

**Adicionar re-export:**
```typescript
export { generateIncidentReportHtml } from './incident-report-renderer.js';
```

**Manter no arquivo original:**
- Interface `IncidentEvent` (exportada)
- Interface `IncidentReport` (exportada)
- Constantes `SEVERITY_ORDER`, `TYPE_ORDER`, `FAIL_RATE_THRESHOLD`, `REGRESSION_COUNT_THRESHOLD` (privadas)
- Função `getEpicsFromInputs()` (privada)
- Função `buildIncidentReport()` (exportada)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/incident-report.test.ts` — pass
- `npx vitest run shared/__tests__/incident-report.property.test.ts` — pass

**Commit:** `feat(report): extract incident-report-renderer from incident-report`

---

### Tarefa 1.4: Extrair impact-alert-renderer

**Arquivo a criar:** `shared/report/impact-alert-renderer.ts`

**Mover de `impact-alert.ts`:**
- Função `generateImpactAlertHtml()` (linha 240)
- Função `renderAlertCard()` (linha 218)
- Constante `SEVERITY_MAP` (linha 212)

**Imports do novo arquivo:**
```typescript
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from './html-factory.js';
import { buildCss } from './report-styles.js';
import { MetricCard, MetricGrid, Card } from '../primitives/index.js';
import { rootLogger } from '../logger.js';
import type { ImpactAlertResult, AlertSeverity } from './impact-alert.js';
```

**Exports do novo arquivo:**
```typescript
export function generateImpactAlertHtml(
  result: ImpactAlertResult | null | undefined,
  title?: string
): string;
```

**Arquivo a modificar:** `shared/report/impact-alert.ts`

**Remover:**
- Função `generateImpactAlertHtml()` (linha 240-294)
- Função `renderAlertCard()` (linha 218-238)
- Constante `SEVERITY_MAP` (linha 212-216)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`, `Card`

**Adicionar re-export:**
```typescript
export { generateImpactAlertHtml } from './impact-alert-renderer.js';
```

**Manter no arquivo original:**
- Constante `IMPACT_ALERT_PROVENANCE` (exportada)
- Type `AlertSeverity` (exportado)
- Interface `ImpactAlert` (exportada)
- Interface `ImpactAlertResult` (exportada)
- Constantes de threshold (privadas)
- Função `analyzePipelineImpact()` (exportada)
- Todas as funções helper de compute (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/impact-alert.test.ts` — pass
- `npx vitest run shared/__tests__/impact-alert.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/impact-alert.integration.test.ts` — pass

**Commit:** `feat(report): extract impact-alert-renderer from impact-alert`

---

### Tarefa 1.5: Extrair traceability-renderer

**Arquivo a criar:** `shared/report/traceability-renderer.ts`

**Mover de `traceability-matrix.ts`:**
- Função `generateTraceabilityHtml()` (linha 383)
- Função `buildStatusBadge()` (linha 254)
- Função `buildHealthBar()` (linha 259)
- Função `buildTestHtml()` (linha 272)
- Função `buildStoryHtml()` (linha 293)
- Função `buildEpicNodeHtml()` (linha 308)
- Função `buildAwarenessHtml()` (linha 324)
- Constante `TRACEABILITY_CSS` (linha 347)

**Imports do novo arquivo:**
```typescript
import { rootLogger } from '../logger.js';
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from './html-factory.js';
import { buildCss } from './report-styles.js';
import { MetricCard, MetricGrid } from '../primitives/index.js';
import type { TraceabilityResult } from './traceability-matrix.js';
```

**Exports do novo arquivo:**
```typescript
export function generateTraceabilityHtml(
  result: TraceabilityResult | null | undefined,
  title?: string
): string;
```

**Arquivo a modificar:** `shared/report/traceability-matrix.ts`

**Remover:**
- Função `generateTraceabilityHtml()` (linha 383-434)
- Função `buildStatusBadge()` (linha 254-257)
- Função `buildHealthBar()` (linha 259-270)
- Função `buildTestHtml()` (linha 272-291)
- Função `buildStoryHtml()` (linha 293-306)
- Função `buildEpicNodeHtml()` (linha 308-322)
- Função `buildAwarenessHtml()` (linha 324-345)
- Constante `TRACEABILITY_CSS` (linha 347-381)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`

**Adicionar re-export:**
```typescript
export { generateTraceabilityHtml } from './traceability-renderer.js';
```

**Manter no arquivo original:**
- Todas as interfaces (exportadas): `TraceabilityNode`, `TraceabilityResult`, `TraceabilityAwarenessEntity`, `TraceabilityAwarenessCategory`, `TraceabilityAwareness`
- Tipo `TestStatus` (privado)
- Função `buildTraceabilityMatrix()` (exportada)
- Todas as funções helper de compute (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/traceability-matrix.test.ts` — pass
- `npx vitest run shared/__tests__/traceability-matrix.property.test.ts` — pass
- `npx vitest run shared/__tests__/traceability-matrix.robust.test.ts` — pass
- `npx vitest run shared/__tests__/integration/traceability-matrix.integration.test.ts` — pass

**Commit:** `feat(report): extract traceability-renderer from traceability-matrix`

---

### Tarefa 1.6: Extrair flakiness-renderer

**Arquivo a criar:** `shared/report/flakiness-renderer.ts`

**Mover de `flakiness-dashboard.ts`:**
- Função `generateFlakinessHtml()` (linha 133)
- Função `buildFlakinessSummary()` (linha 58)
- Função `buildFlakinessTable()` (linha 75)
- Função `buildSourceQualityBanner()` (linha 163)
- Constante `FLAKINESS_CSS` (linha 127)

**Imports do novo arquivo:**
```typescript
import type { FlakinessEntry, DataHub } from '../types/data-hub.js';
import { rootLogger } from '../logger.js';
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from './html-factory.js';
import { buildCss } from './report-styles.js';
import { MetricCard, MetricGrid, Badge, Sparkline } from '../primitives/index.js';
import type { FlakinessThresholds, FlakinessOptions } from './flakiness-dashboard.js';
```

**Exports do novo arquivo:**
```typescript
export function generateFlakinessHtml(
  flaky: FlakinessEntry[],
  title?: string,
  options?: FlakinessOptions
): string;
```

**Arquivo a modificar:** `shared/report/flakiness-dashboard.ts`

**Remover:**
- Função `generateFlakinessHtml()` (linha 133-161)
- Função `buildFlakinessSummary()` (linha 58-73)
- Função `buildFlakinessTable()` (linha 75-117)
- Função `buildSourceQualityBanner()` (linha 163-180)
- Constante `FLAKINESS_CSS` (linha 127-131)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`, `Badge`, `Sparkline`

**Adicionar re-export:**
```typescript
export { generateFlakinessHtml } from './flakiness-renderer.js';
```

**Manter no arquivo original:**
- Interface `FlakinessThresholds` (exportada)
- Interface `FlakinessOptions` (exportada)
- Constante `DEFAULT_THRESHOLDS` (privada)
- Função `validateThresholds()` (privada)
- Função `filterHighFlakiness()` (exportada)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/flakiness-dashboard.test.ts` — pass
- `npx vitest run shared/__tests__/flakiness-dashboard-html.property.test.ts` — pass

**Commit:** `feat(report): extract flakiness-renderer from flakiness-dashboard`

---

### Tarefa 1.7: Extrair backlog-health-renderer

**Arquivo a criar:** `shared/report/backlog-health-renderer.ts`

**Mover de `backlog-health.ts`:**
- Função `generateBacklogHealthHtml()` (linha 175)
- Função `buildIssueListCapped()` (linha 244)
- Função `buildDensityTable()` (linha 271)

**Imports do novo arquivo:**
```typescript
import { sanitizeHtml } from '../escape.js';
import { Card, MetricCard, MetricGrid, Badge } from '../primitives/index.js';
import type { BacklogHealthResult } from './backlog-health.js';
```

**Exports do novo arquivo:**
```typescript
export function generateBacklogHealthHtml(result: BacklogHealthResult): string;
```

**Arquivo a modificar:** `shared/report/backlog-health.ts`

**Remover:**
- Função `generateBacklogHealthHtml()` (linha 175-242)
- Função `buildIssueListCapped()` (linha 244-269)
- Função `buildDensityTable()` (linha 271-290)
- Imports não mais necessários: `sanitizeHtml`, `Card`, `MetricCard`, `MetricGrid`, `Badge`

**Adicionar re-export:**
```typescript
export { generateBacklogHealthHtml } from './backlog-health-renderer.js';
```

**Manter no arquivo original:**
- Constante `BACKLOG_HEALTH_PROVENANCE` (exportada)
- Interface `BacklogHealthIssue` (exportada)
- Interface `BacklogHealthResult` (exportada)
- Interface `BacklogHealthOptions` (exportada)
- Todas as funções compute (exportadas): `mapJiraIssuesToBacklogHealth`, `analyzeUnassignedIssues`, `analyzeStaleIssues`, `analyzeBugsWithoutTests`, `calculateBacklogScore`, `analyzeBacklogHealth`
- Constantes e funções helper de compute (privadas)

**Nota:** `generateBacklogHealthHtml` retorna um fragment HTML (`<div id="backlog-health">...</div>`), não uma página completa. Não usa `buildHtmlPage`/`buildCss`. É aceitável como seção.

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/backlog-health.test.ts` — pass
- `npx vitest run shared/__tests__/backlog-health.property.test.ts` — pass

**Commit:** `feat(report): extract backlog-health-renderer from backlog-health`

---

## FASE 2: Separar Quality Modules

### Tarefa 2.1: Extrair release-score-renderer

**Arquivo a criar:** `shared/quality/release-score-renderer.ts`

**Mover de `release-score.ts`:**
- Função `generateReleaseScoreHtml()` (linha 143)

**Imports do novo arquivo:**
```typescript
import { buildHtmlPage } from '../report/html-factory.js';
import { buildCss } from '../report/report-styles.js';
import { buildReleaseSection } from '../report/report-sections.js';
import { formatDateISO } from '../date-utils.js';
import type { ReleaseScoreResult } from './release-score.js';
```

**Exports do novo arquivo:**
```typescript
export function generateReleaseScoreHtml(result: ReleaseScoreResult): string;
```

**Arquivo a modificar:** `shared/quality/release-score.ts`

**Remover:**
- Função `generateReleaseScoreHtml()` (linha 143-154)
- Imports não mais necessários: `buildHtmlPage`, `buildCss`, `buildReleaseSection`, `formatDateISO`

**Adicionar re-export:**
```typescript
export { generateReleaseScoreHtml } from './release-score-renderer.js';
```

**Manter no arquivo original:**
- Constante `RELEASE_SCORE_PROVENANCE` (exportada)
- Interface `ReleaseScoreBreakdownEntry` (exportada)
- Interface `ReleaseScoreResult` (exportada)
- Função `calculateReleaseScore()` (exportada)
- Constantes e funções helper (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/release-score.test.ts` — pass
- `npx vitest run shared/__tests__/release-score.property.test.ts` — pass
- `npx vitest run shared/__tests__/release-score.robust.test.ts` — pass

**Commit:** `feat(quality): extract release-score-renderer from release-score`

---

### Tarefa 2.2: Extrair silent-regression-renderer

**Arquivo a criar:** `shared/quality/silent-regression-renderer.ts`

**Mover de `silent-regression.ts`:**
- Função `generateSilentRegressionHtml()` (linha 131)

**Imports do novo arquivo:**
```typescript
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from '../report/html-factory.js';
import { buildCss } from '../report/report-styles.js';
import { MetricCard, MetricGrid, DataTable, Badge } from '../primitives/index.js';
import type { TableColumn, TableRow } from '../primitives/index.js';
import { rootLogger } from '../logger.js';
import type { RegressionResult } from './silent-regression.js';
```

**Exports do novo arquivo:**
```typescript
export function generateSilentRegressionHtml(result: RegressionResult | null | undefined): string;
```

**Arquivo a modificar:** `shared/quality/silent-regression.ts`

**Remover:**
- Função `generateSilentRegressionHtml()` (linha 131-198)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`, `DataTable`, `Badge`, `TableColumn`, `TableRow`

**Adicionar re-export:**
```typescript
export { generateSilentRegressionHtml } from './silent-regression-renderer.js';
```

**Manter no arquivo original:**
- Constante `SILENT_REGRESSION_PROVENANCE` (exportada)
- Interface `RegressionEntry` (exportada)
- Interface `RegressionResult` (exportada)
- Função `detectSilentRegression()` (exportada)
- Constantes e funções helper (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/silent-regression.test.ts` — pass
- `npx vitest run shared/__tests__/silent-regression.property.test.ts` — pass

**Commit:** `feat(quality): extract silent-regression-renderer from silent-regression`

---

### Tarefa 2.3: Extrair defect-trend-renderer

**Arquivo a criar:** `shared/quality/defect-trend-renderer.ts`

**Mover de `defect-trend.ts`:**
- Função `generateDefectTrendHtml()` (linha 151)

**Imports do novo arquivo:**
```typescript
import { rootLogger } from '../logger.js';
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from '../report/html-factory.js';
import { buildCss } from '../report/report-styles.js';
import { MetricCard, MetricGrid } from '../primitives/index.js';
import type { DefectTrendResult } from './defect-trend.js';
```

**Exports do novo arquivo:**
```typescript
export function generateDefectTrendHtml(result: DefectTrendResult | null | undefined, title?: string): string;
```

**Arquivo a modificar:** `shared/quality/defect-trend.ts`

**Remover:**
- Função `generateDefectTrendHtml()` (linha 151-185)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`

**Adicionar re-export:**
```typescript
export { generateDefectTrendHtml } from './defect-trend-renderer.js';
```

**Manter no arquivo original:**
- Interface `DefectTrendPoint` (exportada)
- Interface `DefectTrendResult` (exportada)
- Função `sanitizeNumber()` (privada)
- Função `sanitizeTrendResult()` (exportada)
- Função `aggregateDefectTrends()` (exportada)
- Funções helper (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/defect-trend.test.ts` — pass
- `npx vitest run shared/__tests__/defect-trend-html.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/defect-trend.integration.test.ts` — pass

**Commit:** `feat(quality): extract defect-trend-renderer from defect-trend`

---

### Tarefa 2.4: Extrair defect-seasonality-renderer

**Arquivo a criar:** `shared/quality/defect-seasonality-renderer.ts`

**Mover de `defect-seasonality.ts`:**
- Função `generateSeasonalityHtml()` (linha 225)

**Imports do novo arquivo:**
```typescript
import { rootLogger } from '../logger.js';
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from '../report/html-factory.js';
import { buildCss } from '../report/report-styles.js';
import { MetricCard, MetricGrid, DataTable } from '../primitives/index.js';
import type { TableColumn, TableRow } from '../primitives/index.js';
import type { SeasonalityResult } from './defect-seasonality.js';
```

**Exports do novo arquivo:**
```typescript
export function generateSeasonalityHtml(result: SeasonalityResult | null | undefined, title?: string): string;
```

**Arquivo a modificar:** `shared/quality/defect-seasonality.ts`

**Remover:**
- Função `generateSeasonalityHtml()` (linha 225-268)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`, `DataTable`, `TableColumn`, `TableRow`

**Adicionar re-export:**
```typescript
export { generateSeasonalityHtml } from './defect-seasonality-renderer.js';
```

**Manter no arquivo original:**
- Constantes `DAY_NAMES`, `DAY_SORT_ORDER`, `HOUR_COUNT` (privadas)
- Interface `SeasonalityDay` (exportada)
- Interface `SeasonalityHour` (exportada)
- Interface `SeasonalityResult` (exportada)
- Função `aggregateDefectSeasonality()` (exportada)
- Funções helper (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/defect-seasonality.test.ts` — pass
- `npx vitest run shared/__tests__/defect-seasonality.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/defect-seasonality.integration.test.ts` — pass

**Commit:** `feat(quality): extract defect-seasonality-renderer from defect-seasonality`

---

### Tarefa 2.5: Extrair developer-profile-renderer

**Arquivo a criar:** `shared/quality/developer-profile-renderer.ts`

**Mover de `developer-profile.ts`:**
- Função `generateDeveloperProfileHtml()` (linha 218)

**Imports do novo arquivo:**
```typescript
import { rootLogger } from '../logger.js';
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from '../report/html-factory.js';
import { buildCss } from '../report/report-styles.js';
import { MetricCard, MetricGrid, Badge } from '../primitives/index.js';
import type { DeveloperProfileResult } from './developer-profile.js';
```

**Exports do novo arquivo:**
```typescript
export function generateDeveloperProfileHtml(result: DeveloperProfileResult | null | undefined): string;
```

**Arquivo a modificar:** `shared/quality/developer-profile.ts`

**Remover:**
- Função `generateDeveloperProfileHtml()` (linha 218-269)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`, `Badge`

**Adicionar re-export:**
```typescript
export { generateDeveloperProfileHtml } from './developer-profile-renderer.js';
```

**Manter no arquivo original:**
- Interface `AuthorStat` (exportada)
- Interface `DeveloperProfileResult` (exportada)
- Função `findTopCategory()` (privada)
- Função `buildEmptyResult()` (privada)
- Função `buildDeveloperProfile()` (exportada)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/developer-profile.test.ts` — pass
- `npx vitest run shared/__tests__/developer-profile.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/developer-profile.integration.test.ts` — pass

**Commit:** `feat(quality): extract developer-profile-renderer from developer-profile`

---

### Tarefa 2.6: Extrair pipeline-cost-renderer

**Arquivo a criar:** `shared/quality/pipeline-cost-renderer.ts`

**Mover de `pipeline-cost.ts`:**
- Função `generatePipelineCostHtml()` (linha 127)

**Imports do novo arquivo:**
```typescript
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from '../report/html-factory.js';
import { buildCss } from '../report/report-styles.js';
import { MetricCard, MetricGrid, DataTable } from '../primitives/index.js';
import type { TableColumn, TableRow } from '../primitives/index.js';
import { rootLogger } from '../logger.js';
import type { PipelineCostResult } from './pipeline-cost.js';
```

**Exports do novo arquivo:**
```typescript
export function generatePipelineCostHtml(result: PipelineCostResult | null | undefined): string;
```

**Arquivo a modificar:** `shared/quality/pipeline-cost.ts`

**Remover:**
- Função `generatePipelineCostHtml()` (linha 127-174)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`, `DataTable`, `TableColumn`, `TableRow`

**Adicionar re-export:**
```typescript
export { generatePipelineCostHtml } from './pipeline-cost-renderer.js';
```

**Manter no arquivo original:**
- Constante `DEFAULT_COST_PER_MINUTE` (exportada)
- Interface `PipelineCostEntry` (exportada)
- Interface `PipelineCostResult` (exportada)
- Função `calculatePipelineCost()` (exportada)
- Funções helper (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/pipeline-cost.test.ts` — pass
- `npx vitest run shared/__tests__/pipeline-cost.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/pipeline-cost.integration.test.ts` — pass

**Commit:** `feat(quality): extract pipeline-cost-renderer from pipeline-cost`

---

### Tarefa 2.7: Extrair suite-optimization-renderer

**Arquivo a criar:** `shared/quality/suite-optimization-renderer.ts`

**Mover de `suite-optimization.ts`:**
- Função `generateOptimizationHtml()` (linha 157)

**Imports do novo arquivo:**
```typescript
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from '../report/html-factory.js';
import { Container, Section } from '../primitives/layout.js';
import { MetricCard, MetricGrid } from '../primitives/card.js';
import { Badge, SeverityBadge } from '../primitives/badge.js';
import { DataTable, type TableColumn, type TableRow } from '../primitives/table.js';
import { buildCss } from '../report/report-styles.js';
import { rootLogger } from '../logger.js';
import { extractErrorMessage } from '../ui/prompt-errors.js';
import type { OptimizationResult } from './suite-optimization.js';
```

**Exports do novo arquivo:**
```typescript
export function generateOptimizationHtml(result: OptimizationResult | null | undefined): string;
```

**Arquivo a modificar:** `shared/quality/suite-optimization.ts`

**Remover:**
- Função `generateOptimizationHtml()` (linha 157-225)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `Container`, `Section`, `MetricCard`, `MetricGrid`, `Badge`, `SeverityBadge`, `DataTable`, `TableColumn`, `TableRow`, `buildCss`, `extractErrorMessage`

**Adicionar re-export:**
```typescript
export { generateOptimizationHtml } from './suite-optimization-renderer.js';
```

**Manter no arquivo original:**
- Type `OptimizationAction` (exportado)
- Type `OptimizationImpact` (exportado)
- Interface `OptimizationEntry` (exportada)
- Interface `OptimizationResult` (exportada)
- Função `analyzeSuiteOptimization()` (exportada)
- Constantes e funções helper (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/suite-optimization.test.ts` — pass
- `npx vitest run shared/__tests__/suite-optimization.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/suite-optimization.integration.test.ts` — pass

**Commit:** `feat(quality): extract suite-optimization-renderer from suite-optimization`

---

### Tarefa 2.8: Extrair cross-squad-benchmark-renderer

**Arquivo a criar:** `shared/quality/cross-squad-benchmark-renderer.ts`

**Mover de `cross-squad-benchmark.ts`:**
- Função `generateBenchmarkHtml()` (linha 219)

**Imports do novo arquivo:**
```typescript
import { sanitizeHtml } from '../sanitize.js';
import { buildHtmlPage, buildErrorPage } from '../report/html-factory.js';
import { buildCss } from '../report/report-styles.js';
import { formatDateISO } from '../date-utils.js';
import { Card, MetricCard, MetricGrid, Badge, DataTable } from '../primitives/index.js';
import type { TableColumn, TableRow } from '../primitives/index.js';
import { rootLogger } from '../logger.js';
import type { CrossSquadResult } from './cross-squad-benchmark.js';
```

**Exports do novo arquivo:**
```typescript
export function generateBenchmarkHtml(result: CrossSquadResult | null | undefined, title?: string): string;
```

**Arquivo a modificar:** `shared/quality/cross-squad-benchmark.ts`

**Remover:**
- Função `generateBenchmarkHtml()` (linha 219-285)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `formatDateISO`, `Card`, `MetricCard`, `MetricGrid`, `Badge`, `DataTable`, `TableColumn`, `TableRow`

**Adicionar re-export:**
```typescript
export { generateBenchmarkHtml } from './cross-squad-benchmark-renderer.js';
```

**Manter no arquivo original:**
- Constante `BENCHMARK_PROVENANCE` (exportada)
- Type `Trend` (privada)
- Interface `SquadBenchmark` (exportada)
- Interface `CrossSquadResult` (exportada)
- Função `computeCrossSquadBenchmark()` (exportada)
- Constantes e funções helper (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/cross-squad-benchmark.test.ts` — pass
- `npx vitest run shared/__tests__/cross-squad-benchmark.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/cross-squad-benchmark.integration.test.ts` — pass

**Commit:** `feat(quality): extract cross-squad-benchmark-renderer from cross-squad-benchmark`

---

### Tarefa 2.9: Extrair requirement-score-renderer

**Arquivo a criar:** `shared/quality/requirement-score-renderer.ts`

**Mover de `requirement-score.ts`:**
- Função `generateRequirementScoreHtml()` (linha 220)

**Imports do novo arquivo:**
```typescript
import { sanitizeHtml } from '../escape.js';
import { buildHtmlPage, buildErrorPage } from '../report/html-factory.js';
import { buildCss } from '../report/report-styles.js';
import { MetricCard, MetricGrid, DataTable } from '../primitives/index.js';
import type { TableColumn, TableRow } from '../primitives/index.js';
import { rootLogger } from '../logger.js';
import type { RequirementScoreResult } from './requirement-score.js';
```

**Exports do novo arquivo:**
```typescript
export function generateRequirementScoreHtml(result: RequirementScoreResult | null | undefined): string;
```

**Arquivo a modificar:** `shared/quality/requirement-score.ts`

**Remover:**
- Função `generateRequirementScoreHtml()` (linha 220-288)
- Imports não mais necessários: `sanitizeHtml`, `buildHtmlPage`, `buildErrorPage`, `buildCss`, `MetricCard`, `MetricGrid`, `DataTable`, `TableColumn`, `TableRow`

**Adicionar re-export:**
```typescript
export { generateRequirementScoreHtml } from './requirement-score-renderer.js';
```

**Manter no arquivo original:**
- Constante `REQUIREMENT_SCORE_PROVENANCE` (exportada)
- Interface `RequirementScoreEntry` (exportada)
- Interface `RequirementScoreResult` (exportada)
- Função `calculateRequirementScores()` (exportada)
- Constantes e funções helper (privadas)

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run shared/__tests__/requirement-score.test.ts` — pass
- `npx vitest run shared/__tests__/requirement-score.property.test.ts` — pass
- `npx vitest run shared/__tests__/integration/requirement-score.integration.test.ts` — pass

**Commit:** `feat(quality): extract requirement-score-renderer from requirement-score`

---

## FASE 5: Atualizar Orquestradores

### Tarefa 5.1: Atualizar schedule-handler.ts

**Arquivo a modificar:** `git_triggers/schedule-handler.ts`

**Mudança:** Os imports já apontam para os barrels (que re-exportam). Nenhuma mudança de import necessária — os barrels re-exportam tudo.

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run git_triggers/__tests__/schedule-handler.test.ts` — pass

**Commit:** `refactor(schedule): verify schedule-handler barrel compatibility`

---

### Tarefa 5.2: Atualizar interactive-mode.ts

**Arquivo a modificar:** `git_triggers/interactive-mode.ts`

**Mudança:** Os imports já apontam para os barrels. Nenhuma mudança de import necessária.

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run git_triggers/__tests__/interactive-mode.test.ts` — pass

**Commit:** `refactor(interactive): verify interactive-mode barrel compatibility`

---

### Tarefa 5.3: Atualizar batch-mode.ts e pipeline-handler.ts

**Arquivo a modificar:** `git_triggers/batch-mode.ts`

**Mudança:** Import de `generateFlakinessHtml` já aponta para barrel. Nenhuma mudança necessária.

**Arquivo a modificar:** `git_triggers/pipeline-handler.ts`

**Mudança:** Verificar imports de `renderPipelineHealthHtml` (já separado). Nenhuma mudança necessária.

**Verificação:**
- `npx tsc --noEmit` — 0 erros
- `npx vitest run git_triggers/__tests__/batch-mode.test.ts` — pass
- `npx vitest run git_triggers/__tests__/pipeline-health.test.ts` — pass

**Commit:** `refactor(batch): verify batch-mode and pipeline-handler barrel compatibility`

---

## FASE 6: Validar Integração

### Tarefa 6.1: Rodar todos os testes unitários

**Comando:**
```bash
npx vitest run shared/__tests__/
```

**Esperado:** 100% pass

**Commit:** Nenhum (validação apenas)

---

### Tarefa 6.2: Rodar todos os testes de integração

**Comando:**
```bash
npx vitest run shared/__tests__/integration/
```

**Esperado:** 100% pass

**Commit:** Nenhum (validação apenas)

---

### Tarefa 6.3: Rodar testes de git_triggers

**Comando:**
```bash
npx vitest run git_triggers/__tests__/
```

**Esperado:** 100% pass

**Commit:** Nenhum (validação apenas)

---

### Tarefa 6.4: Rodar testes de jira_management

**Comando:**
```bash
npx vitest run jira_management/commands/__tests__/
```

**Esperado:** 100% pass

**Commit:** Nenhum (validação apenas)

---

### Tarefa 6.5: Rodar testes e2e

**Comando:**
```bash
npx vitest run e2e/__tests__/
```

**Esperado:** 100% pass

**Commit:** Nenhum (validação apenas)

---

### Tarefa 6.6: Typecheck completo

**Comando:**
```bash
npx tsc --noEmit
```

**Esperado:** 0 erros

**Commit:** Nenhum (validação apenas)

---

### Tarefa 6.7: Lint completo

**Comando:**
```bash
npm run lint
```

**Esperado:** 0 violações

**Commit:** Nenhum (validação apenas)

---

## FASE 7: Sanitização Final

### Tarefa 7.1: Verificar deadcode

**Comando:**
```bash
npx ts-prune --project tsconfig.json | grep -E "shared/report/|shared/quality/"
```

**Esperado:** 0 exports não usados (além de barrels que re-exportam)

**Ação se encontrar deadcode:** Remover função órfã e commitar.

**Commit (se aplicável):** `chore: remove deadcode from report/quality modules`

---

### Tarefa 7.2: Verificar separação backend/HTML

**Comando:**
```bash
grep -r "html-factory\|report-styles" shared/quality/*.ts | grep -v renderer | grep -v "\.d\.ts"
```

**Esperado:** 0 resultados (nenhum compute module importa HTML)

**Commit:** Nenhum (validação apenas)

---

### Tarefa 7.3: Verificar barrel exports

**Para cada módulo extraído, verificar que o barrel re-exporta compute + render:**

```bash
# Exemplo para ai-effectiveness
grep -c "export.*computeAiEffectiveness" shared/report/ai-effectiveness.ts
grep -c "export.*generateAiEffectivenessHtml" shared/report/ai-effectiveness.ts
```

**Esperado:** 1 para cada export

**Commit:** Nenhum (validação apenas)

---

### Tarefa 7.4: Verificar imports circulares

**Comando:**
```bash
# Nenhum renderer deve importar outro renderer
grep -r "from.*-renderer" shared/report/*-renderer.ts shared/quality/*-renderer.ts
```

**Esperado:** 0 resultados

**Commit:** Nenhum (validação apenas)

---

### Tarefa 7.5: Verificar que nenhum renderer importa outro renderer

**Comando:**
```bash
for f in shared/report/*-renderer.ts shared/quality/*-renderer.ts; do
  echo "=== $f ==="
  grep "from.*-renderer" "$f" || echo "OK"
done
```

**Esperado:** "OK" para cada arquivo

**Commit:** Nenhum (validação apenas)

---

## Resumo de Arquivos Criados (16 renderers)

| # | Arquivo | Módulo Origem |
|---|---------|---------------|
| 1 | `shared/report/ai-effectiveness-renderer.ts` | ai-effectiveness.ts |
| 2 | `shared/report/ai-comparison-renderer.ts` | ai-comparison.ts |
| 3 | `shared/report/incident-report-renderer.ts` | incident-report.ts |
| 4 | `shared/report/impact-alert-renderer.ts` | impact-alert.ts |
| 5 | `shared/report/traceability-renderer.ts` | traceability-matrix.ts |
| 6 | `shared/report/flakiness-renderer.ts` | flakiness-dashboard.ts |
| 7 | `shared/report/backlog-health-renderer.ts` | backlog-health.ts |
| 8 | `shared/quality/release-score-renderer.ts` | release-score.ts |
| 9 | `shared/quality/silent-regression-renderer.ts` | silent-regression.ts |
| 10 | `shared/quality/defect-trend-renderer.ts` | defect-trend.ts |
| 11 | `shared/quality/defect-seasonality-renderer.ts` | defect-seasonality.ts |
| 12 | `shared/quality/developer-profile-renderer.ts` | developer-profile.ts |
| 13 | `shared/quality/pipeline-cost-renderer.ts` | pipeline-cost.ts |
| 14 | `shared/quality/suite-optimization-renderer.ts` | suite-optimization.ts |
| 15 | `shared/quality/cross-squad-benchmark-renderer.ts` | cross-squad-benchmark.ts |
| 16 | `shared/quality/requirement-score-renderer.ts` | requirement-score.ts |

## Resumo de Arquivos Modificados (16 barrels)

| # | Arquivo | Mudança |
|---|---------|---------|
| 1 | `shared/report/ai-effectiveness.ts` | Remove render, adiciona re-export |
| 2 | `shared/report/ai-comparison.ts` | Remove render, adiciona re-export |
| 3 | `shared/report/incident-report.ts` | Remove render, adiciona re-export |
| 4 | `shared/report/impact-alert.ts` | Remove render, adiciona re-export |
| 5 | `shared/report/traceability-matrix.ts` | Remove render, adiciona re-export |
| 6 | `shared/report/flakiness-dashboard.ts` | Remove render, adiciona re-export |
| 7 | `shared/report/backlog-health.ts` | Remove render, adiciona re-export |
| 8 | `shared/quality/release-score.ts` | Remove render, adiciona re-export |
| 9 | `shared/quality/silent-regression.ts` | Remove render, adiciona re-export |
| 10 | `shared/quality/defect-trend.ts` | Remove render, adiciona re-export |
| 11 | `shared/quality/defect-seasonality.ts` | Remove render, adiciona re-export |
| 12 | `shared/quality/developer-profile.ts` | Remove render, adiciona re-export |
| 13 | `shared/quality/pipeline-cost.ts` | Remove render, adiciona re-export |
| 14 | `shared/quality/suite-optimization.ts` | Remove render, adiciona re-export |
| 15 | `shared/quality/cross-squad-benchmark.ts` | Remove render, adiciona re-export |
| 16 | `shared/quality/requirement-score.ts` | Remove render, adiciona re-export |

## FASE 8: Atualizar Documentação

### Tarefa 8.1: Atualizar TECHDOC.md — Seção de Arquitetura

**Arquivo a modificar:** `docs/TECHDOC.md`

**Seção a modificar:** `## ARCHITECTURE` → `### Layered Diagram`

**Mudança:** Atualizar o diagrama de camadas para refletir a separação compute/render:

**Texto atual (linha 52-56):**
```
├─────────────────────────────────────────────────────────────┤
│                    shared/                                   │
│  config-accessor, logger, http-client, jira-client,         │
│  llm-client (6 tiers), state, store, metrics, temp-dir,    │
│  invariants (13), palette, prompts, validation,             │
│  reports, markdown, coverage, health-score                  │
```

**Novo texto:**
```
├─────────────────────────────────────────────────────────────┤
│                    shared/                                   │
│  config-accessor, logger, http-client, jira-client,         │
│  llm-client (6 tiers), state, store, metrics, temp-dir,    │
│  invariants (13), palette, prompts, validation,             │
│  reports (compute + renderers), markdown, coverage,         │
│  health-score, quality dashboards                           │
```

**Commit:** `docs: update TECHDOC architecture diagram for compute/render separation`

---

### Tarefa 8.2: Atualizar TECHDOC.md — Tabela de Módulos shared/

**Arquivo a modificar:** `docs/TECHDOC.md`

**Seção a modificar:** `### shared/` (tabela de módulos, linhas 808-826)

**Mudança:** Atualizar a tabela para refletir a separação. Para cada módulo que foi extraído, atualizar a descrição e as funções listadas.

**Linhas a modificar:**

| Linha | Campo Atual | Novo Campo |
|-------|-------------|------------|
| 808 | `defect-trend.ts` \| Defect trend dashboard HTML report \| `aggregateDefectTrends()`, `generateDefectTrendHtml()` | `defect-trend.ts` \| Defect trend computation + barrel \| `aggregateDefectTrends()` \| + `defect-trend-renderer.ts` \| HTML rendering \| `generateDefectTrendHtml()` |
| 809 | `flakiness-dashboard.ts` \| Flakiness dashboard HTML report \| `filterHighFlakiness()`, `generateFlakinessHtml()` | `flakiness-dashboard.ts` \| Flakiness computation + barrel \| `filterHighFlakiness()` \| + `flakiness-renderer.ts` \| HTML rendering \| `generateFlakinessHtml()` |
| 810 | `defect-seasonality.ts` \| Defect seasonality dashboard HTML report \| `aggregateDefectSeasonality()`, `generateSeasonalityHtml()` | `defect-seasonality.ts` \| Seasonality computation + barrel \| `aggregateDefectSeasonality()` \| + `defect-seasonality-renderer.ts` \| HTML rendering \| `generateSeasonalityHtml()` |
| 818 | `cross-squad-benchmark.ts` \| Cross-squad health score leaderboard with trends \| `computeCrossSquadBenchmark()`, `generateBenchmarkHtml()` | `cross-squad-benchmark.ts` \| Benchmark computation + barrel \| `computeCrossSquadBenchmark()` \| + `cross-squad-benchmark-renderer.ts` \| HTML rendering \| `generateBenchmarkHtml()` |
| 819 | `developer-profile.ts` \| Developer profile failure breakdown dashboard \| `buildDeveloperProfile()`, `generateDeveloperProfileHtml()` | `developer-profile.ts` \| Profile computation + barrel \| `buildDeveloperProfile()` \| + `developer-profile-renderer.ts` \| HTML rendering \| `generateDeveloperProfileHtml()` |
| 820 | `report-*.ts` \| Report generation (HTML, sections, tables, charts) \| `ReportGenerator` | `report-*.ts` \| Report infrastructure (HTML factory, sections, tables, charts) \| `buildHtmlPage()`, `buildCss()`, etc. |
| 821 | `silent-regression.ts` \| Silent regression detector \| `detectSilentRegression()`, `generateSilentRegressionHtml()` | `silent-regression.ts` \| Regression computation + barrel \| `detectSilentRegression()` \| + `silent-regression-renderer.ts` \| HTML rendering \| `generateSilentRegressionHtml()` |
| 822 | `ai-effectiveness.ts` \| AI effectiveness dashboard HTML report \| `computeAiEffectiveness()`, `generateAiEffectivenessHtml()` | `ai-effectiveness.ts` \| AI effectiveness computation + barrel \| `computeAiEffectiveness()` \| + `ai-effectiveness-renderer.ts` \| HTML rendering \| `generateAiEffectivenessHtml()` |
| 823 | `ai-comparison.ts` \| AI vs manual test comparison dashboard \| `compareAiVsManual()`, `generateAiComparisonHtml()` | `ai-comparison.ts` \| AI comparison computation + barrel \| `compareAiVsManual()` \| + `ai-comparison-renderer.ts` \| HTML rendering \| `generateAiComparisonHtml()` |
| 824 | `suite-optimization.ts` \| Suite optimization advisor for test duration/flakiness \| `analyzeSuiteOptimization()`, `generateOptimizationHtml()` | `suite-optimization.ts` \| Optimization computation + barrel \| `analyzeSuiteOptimization()` \| + `suite-optimization-renderer.ts` \| HTML rendering \| `generateOptimizationHtml()` |
| 825 | `backlog-health.ts` \| Backlog health analysis (stale, unassigned, untested) \| `analyzeBacklogHealth()`, `generateBacklogHealthHtml()` | `backlog-health.ts` \| Backlog computation + barrel \| `analyzeBacklogHealth()` \| + `backlog-health-renderer.ts` \| HTML rendering (fragment) \| `generateBacklogHealthHtml()` |

**Adicionar novas linhas na tabela (após a linha 825):**

| Linha | Campo |
|-------|-------|
| nova | `incident-report.ts` \| Incident computation + barrel \| `buildIncidentReport()` \| + `incident-report-renderer.ts` \| HTML rendering \| `generateIncidentReportHtml()` |
| nova | `impact-alert.ts` \| Impact alert computation + barrel \| `analyzePipelineImpact()` \| + `impact-alert-renderer.ts` \| HTML rendering \| `generateImpactAlertHtml()` |
| nova | `traceability-matrix.ts` \| Traceability computation + barrel \| `buildTraceabilityMatrix()` \| + `traceability-renderer.ts` \| HTML rendering \| `generateTraceabilityHtml()` |
| nova | `release-score.ts` \| Release score computation + barrel \| `calculateReleaseScore()` \| + `release-score-renderer.ts` \| HTML rendering \| `generateReleaseScoreHtml()` |
| nova | `pipeline-cost.ts` \| Pipeline cost computation + barrel \| `calculatePipelineCost()` \| + `pipeline-cost-renderer.ts` \| HTML rendering \| `generatePipelineCostHtml()` |
| nova | `requirement-score.ts` \| Requirement score computation + barrel \| `calculateRequirementScores()` \| + `requirement-score-renderer.ts` \| HTML rendering \| `generateRequirementScoreHtml()` |

**Commit:** `docs: update TECHDOC module table for compute/render separation`

---

### Tarefa 8.3: Atualizar TECHDOC.md — Adicionar seção de arquitetura compute/render

**Arquivo a modificar:** `docs/TECHDOC.md`

**Local de inserção:** Após a seção `### Key Patterns` (aproximadamente linha 82)

**Novo conteúdo a adicionar:**

```markdown
### Compute/Render Separation Pattern

Cada módulo de relatório/dashboard segue o padrão **barrel + renderer**:

```
módulo.ts              → compute (funções puras) + re-export barrel
módulo-renderer.ts     → render (geração HTML) — importa do barrel
```

**Barrel (módulo.ts):**
- Contém interfaces, tipos, constantes e funções compute
- Re-exporta a função render do `*-renderer.ts`
- NÃO importa `html-factory` nem `report-styles`

**Renderer (módulo-renderer.ts):**
- Contém apenas funções de geração HTML
- Importa `buildHtmlPage`, `buildCss`, primitives
- Importa tipos do barrel via `import type`
- NÃO contém lógica de negócio

**Benefícios:**
- Separação clara entre dados e apresentação
- Compute modules são testáveis sem dependência de HTML
- Renderers podem ser substituídos (ex: SVG, PDF) sem afetar compute
- Imports circulares impossíveis (renderer → barrel, nunca ao contrário)
```

**Commit:** `docs: add compute/render separation pattern to TECHDOC`

---

### Tarefa 8.4: Atualizar 11-pr-report.md — Seção de arquitetura

**Arquivo a modificar:** `docs/11-pr-report.md`

**Seção a modificar:** `## 5. Arquitetura` (se existir) ou adicionar nova seção

**Verificar se a seção menciona:** `pr-report-core.ts`, `report-html.ts`, `report-sections.ts`

**Mudança:** Adicionar nota sobre separação compute/render:

```markdown
### Separação Compute/Render

Os módulos de relatório seguem o padrão barrel + renderer:
- `report-html.ts` — orchestrator (permanece como orquestrador)
- `report-sections.ts` — sections (permanece)
- `report-table.ts` — table builder (permanece)
- `report-chart.ts` — chart renderer (permanece)
- Módulos de dashboard — compute em `*.ts`, render em `*-renderer.ts`

O PR Report (`pr-report-core.ts`) orquestra a geração chamando compute modules
para obter dados e delegando a renderização para o orchestrator `report-html.ts`.
```

**Commit:** `docs: update PR report docs for compute/render separation`

---

### Tarefa 8.5: Atualizar 08-fluxos-completos.md (se aplicável)

**Arquivo a modificar:** `docs/08-fluxos-completos.md`

**Verificar:** Se algum fluxo menciona geração de relatórios HTML diretamente.

**Mudança:** Se mencionar, adicionar nota de que a geração HTML agora é separada do compute.

**Commit (se aplicável):** `docs: update cookbook for compute/render separation`

---

### Tarefa 8.6: Verificar documentação do show-docs

**Arquivo a verificar:** `shared/report/show-docs.ts`

**Verificar:** Se `showDocs()` gera HTML a partir de Markdown. Se sim, verificar se está afetada pela refatoração.

**Esperado:** `showDocs()` não é afetada — ela gera HTML de documentação, não de relatórios de teste.

**Commit:** Nenhum (validação apenas)

---

### Tarefa 8.7: Rodar show-docs para validar documentação

**Comando:**
```bash
npx tsx -e "import { showDocs } from './shared/report/show-docs.js'; showDocs()"
```

**Esperado:** Documentação gerada e aberta no browser sem erros

**Commit:** Nenhum (validação apenas)

---

## Resumo de Commits Estimados

| Fase | Commits | Descrição |
|------|---------|-----------|
| 0 | 2 | Contrato de dados |
| 1 | 14 | Separar report modules (7 × 2 commits) |
| 2 | 18 | Separar quality modules (9 × 2 commits) |
| **3** | **14** | **Restruturação HTML (qualidade 9 critérios)** → ver `.mimocode/plans/1785100000000-html-quality-restructuring.md` |
| **4** | **28** | **Refatoração CSS (consome hooks Fase 3)** → ver `.mimocode/plans/1785100000001-css-quality-restructuring.md` |
| 5 | 3 | Atualizar orquestradores |
| 6 | 0 | Validação (sem commits) |
| 7 | 0-1 | Sanitização (se aplicável) |
| 8 | 4-6 | Atualizar documentação |
| **Total** | **83-86** | |
