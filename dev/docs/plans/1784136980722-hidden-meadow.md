# PLANO DE REORGANIZAÇÃO DO CODEBASE - qa_tools

## Escopo

Reorganização estritamente de sistema de arquivos. Sem refatoração, sem mudanças de implementação.

---

# 1. MAPA DE IMPACTO

## 1.1 Resumo Geral

| Categoria | Arquivos Movidos | Imports Atualizados | Arquivos Config |
|-----------|------------------|---------------------|-----------------|
| Root cleanup | ~16 | ~8 | 2 |
| analytics/ | 18 + 18 testes | 158 | 3 |
| shared/ subdirs | 82 | 531 | 0 |
| **TOTAL** | **~134** | **~697** | **5** |

## 1.2 FASE 1: Root Cleanup - Mapa de Impacto

### 1.2.1 Documentação para internal_docs/

**ATENÇÃO CRÍTICA:** `internal_docs/` está em `.gitignore` (linha 23). Mover arquivos para lá os remove do version control. Solução: desconsiderar `internal_docs/` no .gitignore ou usar nome diferente.

| Arquivo | Refs Código | Refs CI/CD | Risco | Ação Prévia |
|---------|-------------|------------|-------|-------------|
| `CAPABILITIES-ANALYSIS.md` | NENHUMA | NENHUMA | NENHUM | Mover direto |
| `CAPABILITIES-ANALYSIS-2.md` | NENHUMA | NENHUMA | NENHUM | Mover direto |
| `DEBT-ATTACK-PLAN.md` | NENHUMA | NENHUMA | NENHUM | Mover direto |
| `STRATEGIC-PLAN.md` | NENHUMA | NENHUMA | NENHUM | Mover direto |
| `BACKLOG.md` | **3 refs** | NENHUMA | **MÉDIO** | Atualizar refs |
| `BACKLOG_sanitize.md` | Self-refs | NENHUMA | BAIXO | Mover direto |
| `BACKLOG-historico.md` | Self-refs | NENHUMA | BAIXO | Mover direto |
| `FUNCTIONAL-AUDIT-PROGRESS.md` | **1 ref** | NENHUMA | **ALTO** | Atualizar script |
| `FUNCTIONAL-AUDIT-SOP.md` | Redirect stub | NENHUMA | BAIXO | Mover direto |
| `FUNCTIONAL-AUDIT-INTEGRATED-PLAN.md` | Redirect stub | NENHUMA | BAIXO | Mover direto |
| `WEB_STYLE.md` | Doc refs | NENHUMA | BAIXO | Mover direto |

**Refs de código que precisam de atualização:**

1. **`.githooks/pre-commit:16`** - Exclui `BACKLOG.md` do diff scanning: `':!BACKLOG.md'`
2. **`opencode.json:30,40`** - Agent ts-migrator referencia `BACKLOG.md`
3. **`scripts/sop-phase-enforcer.ts:27`** - `const PROGRESS_FILE = 'FUNCTIONAL-AUDIT-PROGRESS.md';` (usa path relativo do cwd)

### 1.2.2 Scripts para scripts/

| Arquivo | Refs package.json | Refs CI/CD | Refs Código | Risco |
|---------|-------------------|------------|-------------|-------|
| `covcheck.cjs` | NENHUMA | NENHUMA | NENHUMA | NENHUM |
| `covscript.cjs` | NENHUMA | NENHUMA | NENHUMA | NENHUM |
| `parse-cov.cjs` | NENHUMA | NENHUMA | NENHUMA | NENHUM |
| `proxy_off.sh` | NENHUMA | NENHUMA | NENHUMA | NENHUM |

**Todos os 4 scripts são órfãos.** Seguro para mover.

### 1.2.3 Templates para jira_management/

| Arquivo | Refs Código | Risco |
|---------|-------------|-------|
| `test_steps_template.csv` | **4 refs** | **ALTO** |
| `test_cases_template.json` | **3 refs** | **ALTO** |

**Refs de código que precisam de atualização:**

1. **`jira_management/commands/case11.ts:47-49`** - `resolveSource()` hardcodes path `'../..'`
2. **`jira_management/csv_resource.ts:237`** - Mensagem de erro referencia template
3. **`jira_management/import-prep-parsers.ts:123`** - Mensagem de erro referencia template
4. **Docs:** `docs/02-jira-management.md`, `docs/04-csv-format.md`, `docs/05-json-format.md`

**ATENÇÃO:** `jira_management/test_steps_template.csv` já existe! Resolver duplicata.

### 1.2.4 Arquivos para DELETAR

| Arquivo | Refs | Risco |
|---------|------|-------|
| `1782507364202-witty-falcon.md` | NENHUMA | NENHUM |
| `*.Zone.Identifier` | NENHUM (não existem) | N/A |
| `eslint-results.json` | NENHUMA | NENHUM |
| `coverage-gap-report.html` | Runtime output only | NENHUM |
| `.security_violations.log` | NENHUMA | NENHUM |
| `tsconfig.json.bak` | NENHUMA (.gitignored) | NENHUM |

### 1.2.5 Temp Directories

| Diretório | Existe | .gitignore | Refs |
|-----------|--------|------------|------|
| `tmp/` | NÃO | SIM | NENHUMA |
| `.tmp/` | NÃO | NÃO | NENHUMA |

**Nenhum diretório existe.** Nada para limpar.

---

## 1.3 FASE 2: analytics/ - Mapa de Impacto

### 1.3.1 Arquivos a Mover (18 fontes + 18 testes)

#### coverage/ (3 arquivos + 1 teste)

| Origem | Destino | Consumers |
|--------|---------|-----------|
| `shared/coverage-gap.ts` | `analytics/coverage/coverage-gap.ts` | 4 |
| `shared/coverage-gap-utils.ts` | `analytics/coverage/coverage-gap-utils.ts` | 0 (só coverage-gap.ts) |
| `shared/generate-coverage-gap-html.ts` | `analytics/coverage/generate-coverage-gap-html.ts` | 4 |
| `shared/coverage-gap.test.ts` | `analytics/__tests__/coverage/coverage-gap.test.ts` | 0 |

**Total consumers:** 4 arquivos únicos

#### reports/ (7 arquivos + 1 teste)

| Origem | Destino | Consumers |
|--------|---------|-----------|
| `shared/bug-report.ts` | `analytics/reports/bug-report.ts` | 7 |
| `shared/incident-report.ts` | `analytics/reports/incident-report.ts` | 3 |
| `shared/developer-profile.ts` | `analytics/reports/developer-profile.ts` | 3 |
| `shared/defect-seasonality.ts` | `analytics/reports/defect-seasonality.ts` | 3 |
| `shared/defect-trend.ts` | `analytics/reports/defect-trend.ts` | 3 |
| `shared/traceability-matrix.ts` | `analytics/reports/traceability-matrix.ts` | 5 |
| `shared/suite-optimization.ts` | `analytics/reports/suite-optimization.ts` | 3 |
| `shared/incident-report.test.ts` | `analytics/__tests__/reports/incident-report.test.ts` | 0 |

**Total consumers:** 7 arquivos únicos

#### dashboards/ (5 arquivos)

| Origem | Destino | Consumers |
|--------|---------|-----------|
| `shared/flakiness-dashboard.ts` | `analytics/dashboards/flakiness-dashboard.ts` | 4 |
| `shared/ai-comparison.ts` | `analytics/dashboards/ai-comparison.ts` | 3 |
| `shared/ai-effectiveness.ts` | `analytics/dashboards/ai-effectiveness.ts` | 3 |
| `shared/backlog-health.ts` | `analytics/dashboards/backlog-health.ts` | 3 |
| `shared/silent-regression.ts` | `analytics/dashboards/silent-regression.ts` | 3 |

**Total consumers:** 4 arquivos únicos

#### scoring/ (5 arquivos)

| Origem | Destino | Consumers |
|--------|---------|-----------|
| `shared/release-score.ts` | `analytics/scoring/release-score.ts` | 5 |
| `shared/requirement-score.ts` | `analytics/scoring/requirement-score.ts` | 3 |
| `shared/test-impact.ts` | `analytics/scoring/test-impact.ts` | 3 |
| `shared/run-comparison.ts` | `analytics/scoring/run-comparison.ts` | 2 |
| `shared/pipeline-cost.ts` | `analytics/scoring/pipeline-cost.ts` | 3 |

**Total consumers:** 5 arquivos únicos

### 1.3.2 Arquivos Consumer (20 únicos)

| Consumer | Imports Atualizados |
|----------|---------------------|
| `git_triggers/interactive-mode.ts` | **28** |
| `git_triggers/schedule-handler.ts` | **13** |
| `git_triggers/pipeline-jira.ts` | 1 |
| `git_triggers/pipeline-handler.test.ts` | 1 |
| `git_triggers/pipeline-jira.test.ts` | 1 |
| `git_triggers/schedule-handler.test.ts` | 1 |
| `git_triggers/batch-mode.ts` | 2 |
| `jira_management/commands/case17.ts` | 1 |
| `jira_management/commands/case19.ts` | 1 |
| `jira_management/commands/case20.ts` | 1 |
| `jira_management/commands/case20.test.ts` | 1 |
| `jira_management/commands/case21.ts` | 1 |
| `jira_management/commands/case21.test.ts` | 2 |
| `jira_management/commands/case22.ts` | 1 |
| `jira_management/commands/case22.test.ts` | 1 |
| `jira_management/commands/case25.ts` | 1 |
| `jira_management/commands/case26.ts` | 1 |
| `jira_management/commands/case27.ts` | 2 |
| `jira_management/dashboard-handlers.test.ts` | 2 |
| `e2e/smoke-pipeline.ts` | 1 |

**Total:** 64 linhas de import em consumers + 94 imports internos = **158 atualizações**

### 1.3.3 Arquivos Mantidos no shared/ (3)

| Arquivo | Motivo |
|---------|--------|
| `shared/bug-report-validator.ts` | Importado por `shared/llm-review.ts:34` |
| `shared/coverage-verifier.ts` | Importado por `shared/llm-review.ts:37` |
| `shared/bug-report.schema.ts` | Importado por `shared/llm-review-prompts.ts:10` |

### 1.3.4 Config Files

| Arquivo | Mudança |
|---------|---------|
| `tsconfig.json` | Adicionar `"analytics/**/*.ts"` ao include |
| `tsconfig.build.json` | Adicionar `"analytics/**/*.ts"` ao include |
| `package.json` | Verificar script depcruise |

---

## 1.4 FASE 3: shared/ Subdiretórios - Mapa de Impacto

### 1.4.1 Resumo por Subdiretório

| Subdiretório | Arquivos | Consumers Externos | Imports Externos | Imports Internos | **Total** |
|---------------|----------|-------------------|------------------|------------------|-----------|
| `shared/ui/` | 12 | 57 | 67 | 63 | **130** |
| `shared/llm/` | 18 | 8 | 19 | 81 | **119** |
| `shared/report/` | 18 | 3 | 5 | 99 | **104** |
| `shared/infra/` | 8 | 36 | 41 | 28 | **69** |
| `shared/validation/` | 14 | 6 | 6 | 59 | **65** |
| `shared/ci/` | 8 | 15 | 14 | 10 | **24** |
| `shared/quality/` | 4 | 7 | 5 | 15 | **20** |
| **TOTAL** | **82** | **~100** | **157** | **355** | **531** |

### 1.4.2 shared/ui/ - Detalhes (MAIOR IMPACTO)

**Arquivos:** prompt.ts, prompt-ui.ts, prompt-format.ts, prompt-errors.ts, prompt-summary.ts, prompt-input.ts, prompt-input-base.ts, prompt-input-editor.ts, prompt-input-filepath.ts, prompt-input-inquirer.ts, splash.ts, spinner.ts, output.ts, palette.ts

**Consumers Externos (57 arquivos):**
- `jira_management/commands/`: 28 arquivos (case01-case27, handlers, test-execution-flow)
- `jira_management/`: 13 arquivos (create_tests, cypress_resource, import-orchestrator, etc.)
- `git_triggers/`: 13 arquivos (batch-mode, interactive-mode, pipeline-handler, etc.)
- `e2e/`: 3 arquivos
- `scripts/`: 1 arquivo
- `setup/`: 2 arquivos

### 1.4.3 shared/llm/ - Detalhes

**Arquivos:** llm-benchmark.ts, llm-cache.ts, llm-client.ts, llm-fallback.ts, llm-fallback-config.ts, llm-fallback-http.ts, llm-metrics.ts, llm-probe.ts, llm-provider-profiles.ts, llm-rate-limiter.ts, llm-review.ts, llm-review-analyzer.ts, llm-review-prompts.ts, llm-review-types.ts, llm-self-consistency.ts, llm-validation.ts, model-adapter.ts, model-discovery.ts, model-resolver.ts

**Consumers Externos (8 arquivos):**
- `e2e/llm-pipeline.test.ts`
- `git_triggers/ai-pr-desc.ts`, `ai-test-impact.ts` (+ testes)
- `jira_management/commands/case18.ts` (+ teste)
- `scripts/smartwizard-llm.ts`, `smartwizard-discovery.ts`, `probe-registry.ts`

**Acoplamento Interno:** 81 imports internos (cadeia llm-client → llm-fallback → llm-fallback-config → llm-fallback-http)

### 1.4.4 shared/report/ - Detalhes

**Arquivos:** html-factory.ts, markdown.ts, markdown-html.ts, markdown-lexer.ts, markdown-nav.ts, markdown-renderer.ts, report-chart.ts, report-diff.ts, report-export.ts, report-generator.ts, report-html.ts, report-scripts.ts, report-sections.ts, report-styles.ts, report-table.ts, report-types.ts, report-utils.ts, report-validator.ts

**Consumers Externos (3 arquivos):**
- `e2e/gen-report-complete.ts`
- `git_triggers/batch-mode.ts`, `pipeline-health-renderer.ts`
- `jira_management/import-prep-preview.ts`

**Interno:** 99 imports internos (denso mas isolado externamente)

### 1.4.5 shared/ci/ - Detalhes

**Arquivos:** ci-data.ts, ci-detect.ts, ci-injector.ts, git-metrics-adapter.ts, git-provider-error.ts, git-sha.ts, github-check-run.ts, github-pr-comment.ts

**Consumers Externos (15 arquivos):**
- `git_triggers/`: 10 arquivos (git-provider-base, github-*, gitlab-*, batch-mode, interactive-mode, etc.)
- `jira_management/commands/case17-helpers.ts`
- `setup/main.ts`, `main.test.ts`

### 1.4.6 shared/infra/ - Detalhes

**Arquivos:** circuit-breaker.ts, disk-cache.ts, host-semaphore.ts, http-client.ts, proxy-config.ts, store-backend.ts, temp-dir.ts, tls.ts

**Consumers Externos (36 arquivos):**
- `git_triggers/`: 12 arquivos
- `jira_management/`: 12 arquivos
- `e2e/`: 5 arquivos

**Maior impacto:** `temp-dir.js` (23 consumers externos)

### 1.4.7 shared/quality/ - Detalhes

**Arquivos:** health-score.ts, quality-gate.ts, quality-metrics.ts, quality-suggester.ts

**Consumers Externos (7 arquivos):**
- `git_triggers/interactive-mode.ts`, `schedule-handler.ts`
- `jira_management/commands/case19.ts`, `case26.ts`, `main.ts` (+ testes)

### 1.4.8 shared/validation/ - Detalhes

**Arquivos:** analysis-validator.ts, artifact-validator.ts, bug-report-validator.ts, comparison-schema.ts, comparison-validator.ts, config-schema.ts, config-validator.ts, evidence-validator.ts, pipeline-schema.ts, pipeline-validator.ts, quarantine.ts, shared-invariants.ts, test-case-validator.ts, validation.ts

**Consumers Externos (6 arquivos):**
- `scripts/generate-env-example.ts`
- `e2e/smoke-jira-cloud.test.ts`
- `jira_management/import-prep-parsers.ts`, `csv-import-schema.ts`, `xray-history.ts`
- `git_triggers/batch-mode.ts`

---

# 2. PASSO A PASSO DA IMPLEMENTAÇÃO

## Ordem Cronológica e Lógica

### FASE 0: Preparação (5 min)

1. Criar branch de trabalho: `git checkout -b feat/reorganize-codebase`
2. Verificar estado limpo: `git status`
3. Executar baseline: `npx tsc --noEmit && npx vitest run`

### FASE 1: Root Cleanup (15 min)

#### Passo 1.1: Atualizar .gitignore
```bash
# Remover internal_docs/ do .gitignore para manter versionado
# OU criar diretório com nome diferente (ex: docs/internal/)
```

#### Passo 1.2: Atualizar Referências de Código
1. `.githooks/pre-commit:16` - Atualizar exclusão de `BACKLOG.md`
2. `opencode.json:30,40` - Atualizar referências a `BACKLOG.md`
3. `scripts/sop-phase-enforcer.ts:27` - Atualizar path de `FUNCTIONAL-AUDIT-PROGRESS.md`

#### Passo 1.3: Mover Documentação (sem refs de código)
```bash
git mv CAPABILITIES-ANALYSIS.md internal_docs/
git mv CAPABILITIES-ANALYSIS-2.md internal_docs/
git mv DEBT-ATTACK-PLAN.md internal_docs/
git mv STRATEGIC-PLAN.md internal_docs/
git mv BACKLOG_sanitize.md internal_docs/
git mv BACKLOG-historico.md internal_docs/
git mv WEB_STYLE.md internal_docs/
git mv FUNCTIONAL-AUDIT-SOP.md internal_docs/
git mv FUNCTIONAL-AUDIT-INTEGRATED-PLAN.md internal_docs/
```

#### Passo 1.4: Mover Documentação (com refs de código)
```bash
git mv BACKLOG.md internal_docs/  # Após atualizar refs
git mv FUNCTIONAL-AUDIT-PROGRESS.md internal_docs/  # Após atualizar script
```

#### Passo 1.5: Mover Scripts Órfãos
```bash
git mv covcheck.cjs scripts/
git mv covscript.cjs scripts/
git mv parse-cov.cjs scripts/
git mv proxy_off.sh scripts/
```

#### Passo 1.6: Deletar Artefatos
```bash
git rm 1782507364202-witty-falcon.md
git rm eslint-results.json
git rm coverage-gap-report.html
git rm .security_violations.log
git rm tsconfig.json.bak
```

#### Passo 1.7: Templates (DECISÃO NECESSÁRIA)
**Opção A:** Mover para `jira_management/` e atualizar `case11.ts:resolveSource()`
**Opção B:** Manter no root (recomendado - menos risco)
**Opção C:** Deletar duplicata em `jira_management/` e manter no root

**Recomendação:** Opção C (menor risco)

#### Passo 1.8: Verificação
```bash
npx tsc --noEmit
npx vitest run --reporter=verbose
```

### FASE 2: Criar analytics/ (30 min)

#### Passo 2.1: Criar Estrutura
```bash
mkdir -p analytics/__tests__/coverage
mkdir -p analytics/__tests__/reports
mkdir -p analytics/coverage
mkdir -p analytics/reports
mkdir -p analytics/dashboards
mkdir -p analytics/scoring
```

#### Passo 2.2: Mover Arquivos de coverage/
```bash
git mv shared/coverage-gap.ts analytics/coverage/
git mv shared/coverage-gap-utils.ts analytics/coverage/
git mv shared/generate-coverage-gap-html.ts analytics/coverage/
git mv shared/coverage-gap.test.ts analytics/__tests__/coverage/
```

#### Passo 2.3: Mover Arquivos de reports/
```bash
git mv shared/bug-report.ts analytics/reports/
git mv shared/incident-report.ts analytics/reports/
git mv shared/developer-profile.ts analytics/reports/
git mv shared/defect-seasonality.ts analytics/reports/
git mv shared/defect-trend.ts analytics/reports/
git mv shared/traceability-matrix.ts analytics/reports/
git mv shared/suite-optimization.ts analytics/reports/
git mv shared/incident-report.test.ts analytics/__tests__/reports/
```

#### Passo 2.4: Mover Arquivos de dashboards/
```bash
git mv shared/flakiness-dashboard.ts analytics/dashboards/
git mv shared/ai-comparison.ts analytics/dashboards/
git mv shared/ai-effectiveness.ts analytics/dashboards/
git mv shared/backlog-health.ts analytics/dashboards/
git mv shared/silent-regression.ts analytics/dashboards/
```

#### Passo 2.5: Mover Arquivos de scoring/
```bash
git mv shared/release-score.ts analytics/scoring/
git mv shared/requirement-score.ts analytics/scoring/
git mv shared/test-impact.ts analytics/scoring/
git mv shared/run-comparison.ts analytics/scoring/
git mv shared/pipeline-cost.ts analytics/scoring/
```

#### Passo 2.6: Atualizar Imports Internos (18 arquivos)
Para cada arquivo movido, atualizar imports de `./` para `../shared/`:
- `analytics/coverage/*.ts` → `../shared/logger.js`, `../shared/types.js`, etc.
- `analytics/reports/*.ts` → `../shared/escape.js`, `../shared/html-factory.js`, etc.
- `analytics/dashboards/*.ts` → `../shared/escape.js`, `../shared/primitives/index.js`, etc.
- `analytics/scoring/*.ts` → `../shared/logger.js`, `../shared/llm-client.js`, etc.

**Total:** ~94 linhas de import

#### Passo 2.7: Atualizar Imports dos Consumers (20 arquivos)
Atualizar paths de `../shared/xxx.js` para `../analytics/xxx.js`:
- `git_triggers/interactive-mode.ts` (28 imports)
- `git_triggers/schedule-handler.ts` (13 imports)
- Outros 18 arquivos (23 imports)

**Total:** ~64 linhas de import

#### Passo 2.8: Atualizar Configuração
1. `tsconfig.json` - Adicionar `"analytics/**/*.ts"` ao include
2. `tsconfig.build.json` - Adicionar `"analytics/**/*.ts"` ao include
3. `.dependency-cruiser.js` - Adicionar regras para analytics/

#### Passo 2.9: Verificação
```bash
npx tsc --noEmit
npx vitest run --reporter=verbose
npx depcruise --config .dependency-cruiser.js analytics/ --output-type text
```

### FASE 3: Organizar shared/ (45 min)

#### Passo 3.1: Criar Subdiretórios
```bash
mkdir -p shared/llm
mkdir -p shared/ui
mkdir -p shared/report
mkdir -p shared/quality
mkdir -p shared/ci
mkdir -p shared/infra
mkdir -p shared/validation
```

#### Passo 3.2: Mover para shared/llm/ (18 arquivos)
```bash
git mv shared/llm-*.ts shared/llm/
git mv shared/model-*.ts shared/llm/
```

#### Passo 3.3: Mover para shared/ui/ (14 arquivos)
```bash
git mv shared/prompt*.ts shared/ui/
git mv shared/splash.ts shared/ui/
git mv shared/spinner.ts shared/ui/
git mv shared/output.ts shared/ui/
git mv shared/palette.ts shared/ui/
```

#### Passo 3.4: Mover para shared/report/ (18 arquivos)
```bash
git mv shared/html-factory.ts shared/report/
git mv shared/markdown*.ts shared/report/
git mv shared/report-*.ts shared/report/
```

#### Passo 3.5: Mover para shared/quality/ (4 arquivos)
```bash
git mv shared/health-score.ts shared/quality/
git mv shared/quality-*.ts shared/quality/
```

#### Passo 3.6: Mover para shared/ci/ (8 arquivos)
```bash
git mv shared/ci-*.ts shared/ci/
git mv shared/git-*.ts shared/ci/
git mv shared/github-*.ts shared/ci/
```

#### Passo 3.7: Mover para shared/infra/ (8 arquivos)
```bash
git mv shared/http-client.ts shared/infra/
git mv shared/circuit-breaker.ts shared/infra/
git mv shared/proxy-config.ts shared/infra/
git mv shared/host-semaphore.ts shared/infra/
git mv shared/tls.ts shared/infra/
git mv shared/disk-cache.ts shared/infra/
git mv shared/store-backend.ts shared/infra/
git mv shared/temp-dir.ts shared/infra/
```

#### Passo 3.8: Mover para shared/validation/ (14 arquivos)
```bash
git mv shared/*-validator.ts shared/validation/
git mv shared/validation.ts shared/validation/
git mv shared/*-schema.ts shared/validation/
git mv shared/quarantine.ts shared/validation/
git mv shared/shared-invariants.ts shared/validation/
```

#### Passo 3.9: Atualizar Imports Internos (~355 linhas)
Para cada subdiretório, atualizar imports de `./` para caminhos relativos corretos.

#### Passo 3.10: Atualizar Imports Externos (~157 linhas)
Atualizar paths em jira_management/, git_triggers/, e2e/, scripts/, setup/.

#### Passo 3.11: Verificação
```bash
npx tsc --noEmit
npx vitest run --reporter=verbose
npx depcruise --config .dependency-cruiser.js shared/ --output-type text
```

### FASE 4: Verificação Final (10 min)

```bash
# Compilação
npx tsc --noEmit

# Testes
npx vitest run --reporter=verbose

# Lint
npx eslint .

# Dependency cruiser
npx depcruise --config .dependency-cruiser.js . --output-type text

# Verificar estrutura
find analytics/ -name "*.ts" | wc -l  # Deve ser ~18
find shared/llm/ -name "*.ts" | wc -l  # Deve ser ~18
find shared/ui/ -name "*.ts" | wc -l   # Deve ser ~14
```

---

# 3. ESTRATÉGIA DE SANITIZAÇÃO

## 3.1 Objetivos

1. **Deadcode:** Remover código não utilizado após movimentação
2. **Redundâncias:** Eliminar duplicatas criadas pelo movimento
3. **Resquícios Obsoletos:** Limpar referências quebradas e paths antigos

## 3.2 Checkpoints de Sanitização

### Checkpoint S1: Após Root Cleanup (Fase 1)
- [ ] Verificar que nenhum script quebrou
- [ ] Verificar que docs movidos não são referenciados por código
- [ ] Verificar que templates duplicados foram resolvidos
- [ ] Executar `npx tsc --noEmit`

### Checkpoint S2: Após analytics/ (Fase 2)
- [ ] Verificar que nenhum import quebrou
- [ ] Verificar que arquivos mantidos no shared/ não foram movidos acidentalmente
- [ ] Verificar que dependências circulares não foram criadas
- [ ] Executar `npx vitest run`
- [ ] Executar `npx depcruise --config .dependency-cruiser.js analytics/`

### Checkpoint S3: Após shared/ subdirs (Fase 3)
- [ ] Verificar que imports internos estão corretos
- [ ] Verificar que imports externos estão corretos
- [ ] Verificar que barrel files (index.ts) estão atualizados
- [ ] Verificar que testes passam
- [ ] Executar `npx depcruise --config .dependency-cruiser.js shared/`

## 3.3 Ferramentas de Sanitização

### 3.3.1 Detecção de Imports Quebrados
```bash
# TypeScript compiler detecta imports quebrados
npx tsc --noEmit

# Gera lista de erros
npx tsc --noEmit 2>&1 | grep "Cannot find module"
```

### 3.3.2 Detecção de Código Não Utilizado
```bash
# Knip detecta exports não utilizados
npx knip

# Relatório de unused exports
npx knip --reporter compact
```

### 3.3.3 Detecção de Dependências Circulares
```bash
# Dependency cruiser detecta ciclos
npx depcruise --config .dependency-cruiser.js . --output-type text | grep -i cycle
```

### 3.3.4 Detecção de Paths Obsoletos
```bash
# Grep por paths antigos
grep -r "shared/coverage-gap" --include="*.ts" .
grep -r "shared/llm-client" --include="*.ts" .
grep -r "shared/prompt.js" --include="*.ts" .
```

## 3.4 Regras de Sanitização

### Regra 1: Nenhum Import Quebrado
- Todo import deve resolver corretamente
- `npx tsc --noEmit` deve retornar 0 erros

### Regra 2: Nenhum Path Obsoletos
- Nenhum arquivo deve referenciar paths antigos
- `grep -r "shared/coverage-gap" .` deve retornar vazio

### Regra 3: Nenhuma Dependência Circular
- `analytics/` não pode importar de `jira_management/` ou `git_triggers/`
- `shared/` não pode importar de `analytics/`

### Regra 4: Cobertura de Testes
- Todos os testes devem passar
- Coverage não pode diminuir significativamente

## 3.5 Plano de Contingência

### Se `npx tsc --noEmit` falhar:
1. Identificar imports quebrados
2. Corrigir paths
3. Repetir verificação

### Se `npx vitest run` falhar:
1. Identificar testes que falharam
2. Verificar se é problema de import ou de lógica
3. Corrigir apenas imports (não lógica)

### Se `npx depcruise` reportar violações:
1. Identificar violações de camada
2. Verificar se é import legítimo ou erro
3. Atualizar regras se necessário

---

# 4. PLANO DE AUDITORIA

## 4.1 Critérios Objetivos de Aceitação

### Critério 1: Compilação TypeScript
```bash
npx tsc --noEmit
# Esperado: 0 erros
```

### Critério 2: Testes Unitários
```bash
npx vitest run --reporter=verbose
# Esperado: Todos passam
```

### Critério 3: Lint
```bash
npx eslint .
# Esperado: 0 erros (warnings aceitáveis)
```

### Critério 4: Dependency Cruiser
```bash
npx depcruise --config .dependency-cruiser.js . --output-type text
# Esperado: 0 violations
```

### Critério 5: Estrutura do Projeto
```bash
# analytics/ existe e tem 18 arquivos
find analytics/ -name "*.ts" | wc -l | xargs test 18 -eq

# shared/llm/ existe e tem ~18 arquivos
find shared/llm/ -name "*.ts" | wc -l | xargs test 18 -eq

# shared/ui/ existe e tem ~14 arquivos
find shared/ui/ -name "*.ts" | wc -l | xargs test 14 -eq

# Nenhum arquivo movido fica no local original
test ! -f shared/coverage-gap.ts
test ! -f shared/bug-report.ts
test ! -f shared/llm-client.ts
test ! -f shared/prompt.ts
```

### Critério 6: Sem Imports Obsoletos
```bash
# Nenhuma referência a paths antigos
grep -r "from.*shared/coverage-gap" --include="*.ts" . | wc -l | xargs test 0 -eq
grep -r "from.*shared/llm-client" --include="*.ts" . | wc -l | xargs test 0 -eq
grep -r "from.*shared/prompt.js" --include="*.ts" . | wc -l | xargs test 0 -eq
```

### Critério 7: Cobertura de Código
```bash
# Coverage não pode diminuir mais que 1%
# (Comparar com baseline antes da reorganização)
npx vitest run --coverage
```

## 4.2 Checklist de Validação

### Pré-Implementação
- [ ] Baseline capturado (tsc, vitest, eslint, depcruise)
- [ ] Branch de trabalho criada
- [ ] Estado limpo do git

### Fase 1: Root Cleanup
- [ ] .gitignore atualizado
- [ ] Referências de código atualizadas (3 arquivos)
- [ ] Documentação movida (11 arquivos)
- [ ] Scripts movidos (4 arquivos)
- [ ] Artefatos deletados (6 arquivos)
- [ ] `npx tsc --noEmit` = 0 erros
- [ ] `npx vitest run` = todos passam

### Fase 2: analytics/
- [ ] Estrutura criada (analytics/coverage/, reports/, dashboards/, scoring/)
- [ ] 18 arquivos movidos
- [ ] Imports internos atualizados (~94 linhas)
- [ ] Imports externos atualizados (~64 linhas)
- [ ] tsconfig.json atualizado
- [ ] .dependency-cruiser.js atualizado
- [ ] `npx tsc --noEmit` = 0 erros
- [ ] `npx vitest run` = todos passam
- [ ] `npx depcruise analytics/` = 0 violations

### Fase 3: shared/ subdirs
- [ ] Subdiretórios criados (llm/, ui/, report/, quality/, ci/, infra/, validation/)
- [ ] 82 arquivos movidos
- [ ] Imports internos atualizados (~355 linhas)
- [ ] Imports externos atualizados (~157 linhas)
- [ ] `npx tsc --noEmit` = 0 erros
- [ ] `npx vitest run` = todos passam
- [ ] `npx depcruise shared/` = 0 violations

### Pós-Implementação
- [ ] `npx tsc --noEmit` = 0 erros
- [ ] `npx vitest run` = todos passam
- [ ] `npx eslint .` = 0 erros
- [ ] `npx depcruise .` = 0 violations
- [ ] Nenhum import obsoleto encontrado
- [ ] Estrutura validada
- [ ] Coverage não diminuiu significativamente
- [ ] Commit realizado com mensagem descritiva

## 4.3 Métricas de Sucesso

| Métrica | Meta | Como Verificar |
|---------|------|----------------|
| Compilação | 0 erros | `npx tsc --noEmit` |
| Testes | 100% passam | `npx vitest run` |
| Lint | 0 erros | `npx eslint .` |
| Dependency Cruiser | 0 violations | `npx depcruise .` |
| Imports obsoletos | 0 | `grep -r` por paths antigos |
| Coverage | Não diminui >1% | Comparar com baseline |
| Arquivos movidos | ~134 | `find` commands |
| Imports atualizados | ~697 | Contagem manual |

## 4.4 Riscos e Mitigações

| Risco | Probabilidade | Impacto | Mitigação |
|-------|---------------|---------|-----------|
| Imports quebrados | Média | Alto | `npx tsc --noEmit` após cada fase |
| Dependências circulares | Baixa | Alto | `npx depcruise` após cada fase |
| Testes falham | Média | Alto | `npx vitest run` após cada fase |
| Coverage diminui | Baixa | Médio | Comparar com baseline |
| Paths obsoletos | Média | Médio | `grep -r` por paths antigos |
| Config files quebrados | Baixa | Alto | Verificar tsconfig, depcruise |

## 4.5 Fim do Projeto

O projeto está completo quando:
1. Todos os critérios objetivos são atendidos
2. Todos os itens do checklist estão marcados
3. Todas as métricas de sucesso são alcançadas
4. Nenhum risco materializado permanece não mitigado
5. Commit final realizado com mensagem descritiva

---

# APÊNDICE: Referência Rápida

## Arquivos Críticos (Maior Impacto)

1. `git_triggers/interactive-mode.ts` - 28 imports de analytics/ + shared/ui/
2. `git_triggers/schedule-handler.ts` - 13 imports de analytics/
3. `jira_management/commands/case11.ts` - Path de templates
4. `scripts/sop-phase-enforcer.ts` - Path de FUNCTIONAL-AUDIT-PROGRESS.md
5. `.githooks/pre-commit` - Exclusão de BACKLOG.md

## Comandos de Verificação

```bash
# Pós-cada-fase
npx tsc --noEmit
npx vitest run --reporter=verbose

# Pós- Fase 2
npx depcruise --config .dependency-cruiser.js analytics/ --output-type text

# Pós-Fase 3
npx depcruise --config .dependency-cruiser.js shared/ --output-type text

# Final
npx eslint .
npx depcruise --config .dependency-cruiser.js . --output-type text
npx knip
```
