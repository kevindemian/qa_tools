{
    "name": "password-reset",
    "description": "User story for password reset flow",
    "input": {
        "story": "As a user who forgot my password, I want to reset it via email so that I can regain access to my account.",
        "criteria": [
            "Reset email sent for valid account",
            "Reset link expires after 1 hour",
            "New password must meet strength requirements",
            "Old password cannot be reused"
        ]
    },
    "validate": {
        "type": "json-array",
        "minItems": 3,
        "itemSchema": {
            "title": "string (min 5 chars)",
            "steps": "string[] (min 1 step)",
            "expectedResult": "string (min 10 chars)"
        }
    },
    "coverage": {
        "expectedCriteria": [
            "Reset email sent for valid account",
            "Reset link expires after 1 hour",
            "New password must meet strength requirements",
            "Old password cannot be reused"
        ],
        "numericRanges": [
            { "field": "newPassword", "min": 8, "max": 128, "description": "Password length requirement" },
            { "field": "resetLinkExpiry", "min": 60, "max": 60, "description": "Reset link expires after 1 hour" }
        ]
    }
}
