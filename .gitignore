# === Dependencies ===
node_modules

# === Generated / Temp ===
logs/
reports/*
shared/reports/
temp/
*.log
__pycache__
.mock-boundary-threshold
exported_variables.env
coverage/

# === Local overrides ===
config/*.local.json

# === Sensitive ===
.env
.env.local

# === Dev Tooling (OpenCode agents, commands, briefing) ===
.opencode/
PROJECT.md
internal_docs/
install-opencode.sh

# === Docker Generated ===
shared_docker/docker-compose.yml
shared_docker/.selected_project

# === Root-level stale reports (now all go to reports/) ===
/failure-analysis-*.html

# === Generated output artifacts ===
*.csv
*.html
.audit/

# === Runtime state ===
shared/.local/
# Explicit runtime artifacts (F5/T5.1): generated state, logs and backups
shared/.local/state/*.json
shared/.local/state/*.json.bak
cloud/*.log
archive/
# Root-level runtime state (F5 root cleanup): xdg state home, temp, lint/log artifacts
.qa-tools/
.tmp/
eslint-results.json
job_logs.txt

# === Backup files ===
*.bak
tsconfig.json.bak

# === Temp artifacts ===
tmp/

# === Generated AI code ===
gemini-code-*
*:Zone.Identifier

# === Container build context (local only, not part of the project) ===
.container/

# === Config duplicates (tautest TS vs JSON; only JSON is canonical) ===
tautest.config.ts

# === Windows artifacts ===
nul
.llm-cache/
a.out
qa-quarantine.json
.mimocode/
.qwen/
*.zip
.scannerwork/
.eslintcache
.prettier-cache/

# === Session artifacts (ephemeral, not part of the project) ===
archive/
.stryker-tmp/
research/
.tautest/

# === vitest-affected cache (generated) ===
.vitest-affected/

# === Security scan output (generated) ===
osv-report.json

# === Internal audit/plan docs (maintained locally, not part of distribution) ===
audit/
dev/docs/audit/
dev/docs/compose/
dev/docs/internal/plans/
dev/docs/internal/ARCHITECTURE-CONTRACT.md
dev/docs/internal/MUTATION-TESTING-PLAN.md
dev/docs/internal/reorg-dev-main-plan.md
dev/docs/internal/validation-hook-code-only.md
release_notes/
